buildscript {
  repositories {
    google()
    mavenCentral()
  }
}

apply plugin: "com.android.library"

android {
  namespace "com.legenddesktop.windowmanager"
  compileSdk 35

  defaultConfig {
    minSdk 24
  }
}

dependencies {
  implementation "com.facebook.react:react-android"
}
