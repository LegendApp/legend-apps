---
title: Steps and animation
---

# One point per advance

<Steps transition="fade-up">

- Native UI
- Low memory usage
- Familiar React code

</Steps>

<!-- Advance through the bullets, then go backward. Their positions remain fixed. -->

---

# Direct attention

<Step initial={{ opacity: 1, transform: [{ scale: 1 }] }} states={{ 1: { opacity: 0.3, transform: [{ scale: 0.95 }] }, 3: { opacity: 1, transform: [{ scale: 1 }] } }} transition={{ duration: 400 }}>
  <Text>The original idea stays in place.</Text>
</Step>

<Step at={1} until={3}>Now focus on this point.</Step>
<Step at={2}>This appears while the first point is dimmed.</Step>

<!-- The first advance dims and shrinks the original while revealing a new point. Step 3 restores it and hides the temporary point. -->

---

# Start an effect on cue

<Step at={1}>The animation starts with this reveal.</Step>
<Steps count={3}>{(step) => (
<Effect preset="ripple" active={step >= 1} padding={24}>
  <Text>A shared trigger, with its own clock.</Text>
</Effect>
)}</Steps>
<Step at={2}>The animation continues through this next advance.</Step>

<!-- Navigate backward before step 1 to reset the effect, then forward to replay it. -->
