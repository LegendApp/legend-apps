---
title: Legend Slides Showcase
aspectRatio: 16:9
width: 1920
height: 1080
transition: fade
theme:
  backgroundColor: "#0f172a"
  color: "#f8fafc"
presenter:
  showNext: true
  showNotes: true
---

import { Text, View } from "react-native"
import { InteractiveCounter } from "./components/InteractiveCounter"
import { LifecycleAnimation } from "./components/LifecycleAnimation"
import { SkiaNebula } from "./components/SkiaNebula"
import { TypeGPUBoids } from "./components/TypeGPUBoids"
import { cloudScene } from "./vendor/typegpu-clouds/scene"

export const capabilities = [
  ["Markdown", "Headings, lists, links, and code"],
  ["React Native", "View, Text, Pressable, and Animated"],
  ["Local modules", "TypeScript components beside the deck"],
]

# Legend Slides

Markdown for the story. React Native for everything else.

<View className="mt-10 self-start rounded-2xl bg-cyan-500 px-8 py-4">
  <Text className="text-3xl font-bold text-slate-950">Advanced example deck</Text>
</View>

<!-- Welcome the audience and explain that this deck is compiled from local MDX. -->

---
transition: slide
---

## Markdown remains Markdown

- Inline code such as `const answer = 6 * 7`
- **Strong text**, *emphasis*, and [links](https://reactnative.dev/)
- Fenced code stays visible to the audience

```tsx
function Greeting() {
  return <Text>Hello from MDX</Text>;
}
```

<!-- Point out that inline and fenced code are parsed separately from executable expressions. -->

---

## Local TypeScript components

The counter below is imported from `./components/InteractiveCounter` and owns normal React state.

<InteractiveCounter />

<!-- Click the counter in rehearsal mode to demonstrate that the slide is a live React tree. -->

---
transition: fade
---

## Lifecycle-aware animation

`useSlideLifecycle()` distinguishes the audience's active slide from presenter previews.

<LifecycleAnimation />

<!-- The preview renders the completed state; the audience window animates when this slide becomes active. -->

---

## React Native Skia

The app provides `@shopify/react-native-skia`, so deck-local components can render SkSL effects.

<SkiaNebula />

<!-- This is a live Skia RuntimeEffect. The presenter preview intentionally renders a stable frame. -->

---

## Volumetric TypeGPU clouds

Ray-marched density, self-shadowing, and cinematic light—rendered through React Native WebGPU.

<TypeGPU
  scene={cloudScene}
  width={1120}
  height={560}
  style={{ borderRadius: 28 }}
/>

<!-- The shader utilities are verbatim upstream files; TypeGPU owns the React Native canvas and slide lifecycle. -->

---

## TypeGPU Boids

This second React Native example simulates 500 autonomous agents in a TypeGPU compute shader and renders them as instanced triangles.

<TypeGPUBoids />

<!-- This is Software Mansion Labs' React Native Boids example, using the official @typegpu/react hooks without Reanimated or interaction. -->

---

## Arbitrary HTML

Local HTML pages run directly inside a built-in `Webview` with no server.

<Webview src="./web/kinetic.html" style={{ height: 680, marginTop: 24 }} />

<!-- The HTML, CSS, and JavaScript in this page are loaded straight from the deck directory. -->

---

## React DOM components

Point `component` at a local file and the deck compiler supplies React DOM and bundles it into the page.

<Webview
  component="./web/WebParticleField.tsx"
  props={{ color: "#22d3ee", count: 90, label: "React DOM inside React Native" }}
  style={{ height: 680, marginTop: 24 }}
/>

<!-- Props are serialized into the browser runtime; the component and its local imports are bundled with the deck. -->

---

## Arbitrary expressions

<View className="mt-10 flex-row gap-6">
  {capabilities.map(([title, detail], index) => (
    <View key={title} className="flex-1 rounded-3xl border border-slate-700 bg-slate-800 p-8">
      <Text className="text-xl font-bold uppercase tracking-widest text-cyan-300">{String(index + 1).padStart(2, "0")}</Text>
      <Text className="mt-5 text-4xl font-bold text-white">{title}</Text>
      <Text className="mt-4 text-2xl leading-9 text-slate-300">{detail}</Text>
    </View>
  ))}
</View>

<!-- This grid is produced by an ordinary JavaScript array map inside the MDX slide. -->

---
transition: none
---

# Ready to present

Use ← and →, Page Up and Page Down, or Space from either window.

<!-- Select a display in the presenter sidebar, then choose Present. -->
