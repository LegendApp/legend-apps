diff --git a/ios/EnrichedMarkdown.h b/ios/EnrichedMarkdown.h
index 97b03c6..8f0ec92 100644
--- a/ios/EnrichedMarkdown.h
+++ b/ios/EnrichedMarkdown.h
@@ -7,6 +7,11 @@

 NS_ASSUME_NONNULL_BEGIN

+typedef NSString *_Nonnull (^ENRMNativeMarkdownProvider)(NSString *blockId);
+
+void ENRMSetNativeMarkdownProvider(ENRMNativeMarkdownProvider _Nullable provider);
+NSString *ENRMMarkdownForNativeBlockId(NSString *blockId);
+
 @interface EnrichedMarkdown : RCTViewComponentView
 @property (nonatomic, strong) StyleConfig *config;
 - (CGSize)measureSize:(CGFloat)maxWidth;
diff --git a/ios/EnrichedMarkdown.mm b/ios/EnrichedMarkdown.mm
index 9227b78..cd5ab79 100644
--- a/ios/EnrichedMarkdown.mm
+++ b/ios/EnrichedMarkdown.mm
@@ -45,6 +45,30 @@

 using namespace facebook::react;

+static ENRMNativeMarkdownProvider currentNativeMarkdownProvider;
+
+void ENRMSetNativeMarkdownProvider(ENRMNativeMarkdownProvider provider)
+{
+  currentNativeMarkdownProvider = [provider copy];
+}
+
+NSString *ENRMMarkdownForNativeBlockId(NSString *blockId)
+{
+  if (blockId.length == 0 || currentNativeMarkdownProvider == nil) {
+    return @"";
+  }
+  return currentNativeMarkdownProvider(blockId) ?: @"";
+}
+
+static NSString *ENRMMarkdownForProps(const EnrichedMarkdownProps &props)
+{
+  if (!props.nativeMarkdownBlockId.empty()) {
+    NSString *blockId = [[NSString alloc] initWithUTF8String:props.nativeMarkdownBlockId.c_str()];
+    return ENRMMarkdownForNativeBlockId(blockId);
+  }
+  return [[NSString alloc] initWithUTF8String:props.markdown.c_str()] ?: @"";
+}
+
 @interface EMTextSegment : NSObject
 @property (nonatomic, strong) NSArray<MarkdownASTNode *> *nodes;
 + (instancetype)segmentWithNodes:(NSArray<MarkdownASTNode *> *)nodes;
@@ -522,6 +546,20 @@ - (EnrichedMarkdownInternalText *)createTextViewForRenderedSegment:(ENRMRenderRe
     return buildEditMenuForSelection(textView.textStorage, textView.selectedRange, segmentMarkdown, strongSelf->_config,
                                      @[ baseMenu ], customItems);
   }];
+  [view setSelectionDragOutsideHandler:^(NSString *direction, CGFloat windowX, CGFloat windowY) {
+    EnrichedMarkdown *strongSelf = weakSelf;
+    if (!strongSelf) {
+      return;
+    }
+    auto eventEmitter = std::static_pointer_cast<EnrichedMarkdownEventEmitter const>(strongSelf->_eventEmitter);
+    if (eventEmitter) {
+      eventEmitter->onSelectionDragOutside({
+          .direction = std::string([direction UTF8String] ?: ""),
+          .windowX = windowX,
+          .windowY = windowY,
+      });
+    }
+  }];
 #endif

   return view;
@@ -637,7 +675,10 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
     _md4cFlags.latexMath = newViewProps.md4cFlags.latexMath;
     md4cFlagsChanged = YES;
   }
-  BOOL markdownChanged = oldViewProps.markdown != newViewProps.markdown;
+  BOOL markdownChanged =
+      oldViewProps.markdown != newViewProps.markdown ||
+      oldViewProps.nativeMarkdownBlockId != newViewProps.nativeMarkdownBlockId ||
+      oldViewProps.nativeMarkdownRevision != newViewProps.nativeMarkdownRevision;
   BOOL allowTrailingMarginChanged = newViewProps.allowTrailingMargin != oldViewProps.allowTrailingMargin;

   _enableLinkPreview = newViewProps.enableLinkPreview;
@@ -658,7 +699,7 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
   }

   if (markdownChanged || stylePropChanged || md4cFlagsChanged || allowTrailingMarginChanged) {
-    NSString *markdownString = [[NSString alloc] initWithUTF8String:newViewProps.markdown.c_str()];
+    NSString *markdownString = ENRMMarkdownForProps(newViewProps);
     [self renderMarkdownContent:markdownString];
   }

@@ -740,7 +781,7 @@ - (void)textTapped:(ENRMTapRecognizer *)recognizer
             if (eventEmitter) {
               eventEmitter->onTaskListItemPress({
                   .index = (int)index,
-                  .checked = checked,
+                  .checked = static_cast<bool>(checked),
                   .text = std::string([itemText UTF8String] ?: ""),
               });
             }
diff --git a/ios/EnrichedMarkdownText.mm b/ios/EnrichedMarkdownText.mm
index 239e435..33aedf0 100644
--- a/ios/EnrichedMarkdownText.mm
+++ b/ios/EnrichedMarkdownText.mm
@@ -4,6 +4,7 @@
 #import "ENRMContextMenuTextView+macOS.h"
 #import "ENRMImageAttachment.h"
 #import "ENRMMarkdownParser.h"
+#import "EnrichedMarkdown.h"
 #import "ENRMSpoilerOverlayManager.h"
 #import "ENRMSpoilerTapUtils.h"
 #import "ENRMTailFadeInAnimator.h"
@@ -34,6 +35,15 @@

 using namespace facebook::react;

+static NSString *ENRMMarkdownTextForProps(const EnrichedMarkdownTextProps &props)
+{
+  if (!props.nativeMarkdownBlockId.empty()) {
+    NSString *blockId = [[NSString alloc] initWithUTF8String:props.nativeMarkdownBlockId.c_str()];
+    return ENRMMarkdownForNativeBlockId(blockId);
+  }
+  return [[NSString alloc] initWithUTF8String:props.markdown.c_str()] ?: @"";
+}
+
 @interface EnrichedMarkdownText () <RCTEnrichedMarkdownTextViewProtocol, UITextViewDelegate>
 - (void)setupTextView;
 - (void)renderMarkdownContent:(NSString *)markdownString;
@@ -201,6 +211,20 @@ - (void)setupTextView
     return buildEditMenuForSelection(textView.textStorage, textView.selectedRange, strongSelf->_cachedMarkdown,
                                      strongSelf->_config, @[ baseMenu ], customItems);
   };
+  ((ENRMContextMenuTextView *)_textView).selectionDragOutsideHandler = ^(NSString *direction, CGFloat windowX, CGFloat windowY) {
+    EnrichedMarkdownText *strongSelf = weakSelf;
+    if (!strongSelf) {
+      return;
+    }
+    auto eventEmitter = std::static_pointer_cast<EnrichedMarkdownTextEventEmitter const>(strongSelf->_eventEmitter);
+    if (eventEmitter) {
+      eventEmitter->onSelectionDragOutside({
+          .direction = std::string([direction UTF8String] ?: ""),
+          .windowX = windowX,
+          .windowY = windowY,
+      });
+    }
+  };
 #endif

   ENRMTapRecognizer *tapRecognizer = [[ENRMTapRecognizer alloc] initWithTarget:self action:@selector(textTapped:)];
@@ -444,7 +468,10 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
     _md4cFlags.latexMath = newViewProps.md4cFlags.latexMath;
     md4cFlagsChanged = YES;
   }
-  BOOL markdownChanged = oldViewProps.markdown != newViewProps.markdown;
+  BOOL markdownChanged =
+      oldViewProps.markdown != newViewProps.markdown ||
+      oldViewProps.nativeMarkdownBlockId != newViewProps.nativeMarkdownBlockId ||
+      oldViewProps.nativeMarkdownRevision != newViewProps.nativeMarkdownRevision;
   BOOL allowTrailingMarginChanged = newViewProps.allowTrailingMargin != oldViewProps.allowTrailingMargin;

   _enableLinkPreview = newViewProps.enableLinkPreview;
@@ -471,7 +498,7 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
   }

   if (markdownChanged || stylePropChanged || md4cFlagsChanged || allowTrailingMarginChanged) {
-    NSString *markdownString = [[NSString alloc] initWithUTF8String:newViewProps.markdown.c_str()];
+    NSString *markdownString = ENRMMarkdownTextForProps(newViewProps);
     [self renderMarkdownContent:markdownString];
   }

@@ -524,7 +551,7 @@ - (void)textTapped:(ENRMTapRecognizer *)recognizer
             if (eventEmitter) {
               eventEmitter->onTaskListItemPress({
                   .index = (int)index,
-                  .checked = checked,
+                  .checked = static_cast<bool>(checked),
                   .text = std::string([itemText UTF8String] ?: ""),
               });
             }
diff --git a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.cpp b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.cpp
index ea444f1..7c7c7dd 100644
--- a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.cpp
+++ b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.cpp
@@ -54,6 +54,17 @@ payload.setProperty(runtime, "selectionEnd", event.selectionEnd);
 }


+void EnrichedMarkdownEventEmitter::onSelectionDragOutside(OnSelectionDragOutside event) const {
+  dispatchEvent("selectionDragOutside", [event=std::move(event)](jsi::Runtime &runtime) {
+    auto payload = jsi::Object(runtime);
+    payload.setProperty(runtime, "direction", event.direction);
+    payload.setProperty(runtime, "windowX", event.windowX);
+    payload.setProperty(runtime, "windowY", event.windowY);
+    return payload;
+  });
+}
+
+
 void EnrichedMarkdownTextInputEventEmitter::onChangeText(OnChangeText event) const {
   dispatchEvent("changeText", [event=std::move(event)](jsi::Runtime &runtime) {
     auto payload = jsi::Object(runtime);
@@ -82,6 +93,27 @@ payload.setProperty(runtime, "end", event.end);
 }


+void EnrichedMarkdownTextInputEventEmitter::onSelectionDragOutside(OnSelectionDragOutside event) const {
+  dispatchEvent("selectionDragOutside", [event=std::move(event)](jsi::Runtime &runtime) {
+    auto payload = jsi::Object(runtime);
+    payload.setProperty(runtime, "direction", event.direction);
+    payload.setProperty(runtime, "windowX", event.windowX);
+    payload.setProperty(runtime, "windowY", event.windowY);
+    return payload;
+  });
+}
+
+
+void EnrichedMarkdownTextInputEventEmitter::onVerticalNavigationOutside(OnVerticalNavigationOutside event) const {
+  dispatchEvent("verticalNavigationOutside", [event=std::move(event)](jsi::Runtime &runtime) {
+    auto payload = jsi::Object(runtime);
+    payload.setProperty(runtime, "direction", event.direction);
+    payload.setProperty(runtime, "preferredX", event.preferredX);
+    return payload;
+  });
+}
+
+
 void EnrichedMarkdownTextInputEventEmitter::onChangeState(OnChangeState event) const {
   dispatchEvent("changeState", [event=std::move(event)](jsi::Runtime &runtime) {
     auto payload = jsi::Object(runtime);
@@ -271,4 +303,15 @@ payload.setProperty(runtime, "selectionEnd", event.selectionEnd);
   });
 }

+
+void EnrichedMarkdownTextEventEmitter::onSelectionDragOutside(OnSelectionDragOutside event) const {
+  dispatchEvent("selectionDragOutside", [event=std::move(event)](jsi::Runtime &runtime) {
+    auto payload = jsi::Object(runtime);
+    payload.setProperty(runtime, "direction", event.direction);
+    payload.setProperty(runtime, "windowX", event.windowX);
+    payload.setProperty(runtime, "windowY", event.windowY);
+    return payload;
+  });
+}
+
 } // namespace facebook::react
diff --git a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.h b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.h
index 797de6a..2c34bb4 100644
--- a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.h
+++ b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/EventEmitters.h
@@ -37,6 +37,12 @@ class EnrichedMarkdownEventEmitter : public ViewEventEmitter {
     int selectionStart;
     int selectionEnd;
     };
+
+  struct OnSelectionDragOutside {
+      std::string direction;
+    double windowX;
+    double windowY;
+    };
   void onLinkPress(OnLinkPress value) const;

   void onLinkLongPress(OnLinkLongPress value) const;
@@ -44,6 +50,8 @@ class EnrichedMarkdownEventEmitter : public ViewEventEmitter {
   void onTaskListItemPress(OnTaskListItemPress value) const;

   void onContextMenuItemPress(OnContextMenuItemPress value) const;
+
+  void onSelectionDragOutside(OnSelectionDragOutside value) const;
 };
 class EnrichedMarkdownTextInputEventEmitter : public ViewEventEmitter {
  public:
@@ -62,6 +70,17 @@ class EnrichedMarkdownTextInputEventEmitter : public ViewEventEmitter {
     int end;
     };

+  struct OnSelectionDragOutside {
+      std::string direction;
+    double windowX;
+    double windowY;
+    };
+
+  struct OnVerticalNavigationOutside {
+      std::string direction;
+    double preferredX;
+    };
+
   struct OnChangeStateBold {
       bool isActive;
     };
@@ -176,6 +195,10 @@ class EnrichedMarkdownTextInputEventEmitter : public ViewEventEmitter {

   void onChangeSelection(OnChangeSelection value) const;

+  void onSelectionDragOutside(OnSelectionDragOutside value) const;
+
+  void onVerticalNavigationOutside(OnVerticalNavigationOutside value) const;
+
   void onChangeState(OnChangeState value) const;

   void onInputFocus(OnInputFocus value) const;
@@ -216,6 +239,12 @@ class EnrichedMarkdownTextEventEmitter : public ViewEventEmitter {
     int selectionStart;
     int selectionEnd;
     };
+
+  struct OnSelectionDragOutside {
+      std::string direction;
+    double windowX;
+    double windowY;
+    };
   void onLinkPress(OnLinkPress value) const;

   void onLinkLongPress(OnLinkLongPress value) const;
@@ -223,5 +252,7 @@ class EnrichedMarkdownTextEventEmitter : public ViewEventEmitter {
   void onTaskListItemPress(OnTaskListItemPress value) const;

   void onContextMenuItemPress(OnContextMenuItemPress value) const;
+
+  void onSelectionDragOutside(OnSelectionDragOutside value) const;
 };
 } // namespace facebook::react
diff --git a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.cpp b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.cpp
index f1fa74a..1f16614 100644
--- a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.cpp
+++ b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.cpp
@@ -20,6 +20,8 @@ EnrichedMarkdownProps::EnrichedMarkdownProps(
     const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

     markdown(convertRawProp(context, rawProps, "markdown", sourceProps.markdown, {})),
+    nativeMarkdownBlockId(convertRawProp(context, rawProps, "nativeMarkdownBlockId", sourceProps.nativeMarkdownBlockId, {})),
+    nativeMarkdownRevision(convertRawProp(context, rawProps, "nativeMarkdownRevision", sourceProps.nativeMarkdownRevision, {0.0})),
     markdownStyle(convertRawProp(context, rawProps, "markdownStyle", sourceProps.markdownStyle, {})),
     enableLinkPreview(convertRawProp(context, rawProps, "enableLinkPreview", sourceProps.enableLinkPreview, {true})),
     selectable(convertRawProp(context, rawProps, "selectable", sourceProps.selectable, {false})),
@@ -30,7 +32,7 @@ EnrichedMarkdownProps::EnrichedMarkdownProps(
     streamingAnimation(convertRawProp(context, rawProps, "streamingAnimation", sourceProps.streamingAnimation, {false})),
     spoilerOverlay(convertRawProp(context, rawProps, "spoilerOverlay", sourceProps.spoilerOverlay, {std::string{"particles"}})),
     contextMenuItems(convertRawProp(context, rawProps, "contextMenuItems", sourceProps.contextMenuItems, {})) {}
-
+
 #ifdef RN_SERIALIZABLE_STATE
 ComponentName EnrichedMarkdownProps::getDiffPropsImplementationTarget() const {
   return "EnrichedMarkdown";
@@ -46,47 +48,55 @@ folly::dynamic EnrichedMarkdownProps::getDiffProps(
     return folly::dynamic::object();
   }
   folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
-
+
   if (markdown != oldProps->markdown) {
     result["markdown"] = markdown;
   }
-
+
+  if (nativeMarkdownBlockId != oldProps->nativeMarkdownBlockId) {
+    result["nativeMarkdownBlockId"] = nativeMarkdownBlockId;
+  }
+
+  if ((nativeMarkdownRevision != oldProps->nativeMarkdownRevision) && !(std::isnan(nativeMarkdownRevision) && std::isnan(oldProps->nativeMarkdownRevision))) {
+    result["nativeMarkdownRevision"] = nativeMarkdownRevision;
+  }
+
   if (markdownStyle != oldProps->markdownStyle) {
     result["markdownStyle"] = toDynamic(markdownStyle);
   }
-
+
   if (enableLinkPreview != oldProps->enableLinkPreview) {
     result["enableLinkPreview"] = enableLinkPreview;
   }
-
+
   if (selectable != oldProps->selectable) {
     result["selectable"] = selectable;
   }
-
+
   if (md4cFlags != oldProps->md4cFlags) {
     result["md4cFlags"] = toDynamic(md4cFlags);
   }
-
+
   if (allowFontScaling != oldProps->allowFontScaling) {
     result["allowFontScaling"] = allowFontScaling;
   }
-
+
   if ((maxFontSizeMultiplier != oldProps->maxFontSizeMultiplier) && !(std::isnan(maxFontSizeMultiplier) && std::isnan(oldProps->maxFontSizeMultiplier))) {
     result["maxFontSizeMultiplier"] = maxFontSizeMultiplier;
   }
-
+
   if (allowTrailingMargin != oldProps->allowTrailingMargin) {
     result["allowTrailingMargin"] = allowTrailingMargin;
   }
-
+
   if (streamingAnimation != oldProps->streamingAnimation) {
     result["streamingAnimation"] = streamingAnimation;
   }
-
+
   if (spoilerOverlay != oldProps->spoilerOverlay) {
     result["spoilerOverlay"] = spoilerOverlay;
   }
-
+
   if (contextMenuItems != oldProps->contextMenuItems) {
     result["contextMenuItems"] = toDynamic(contextMenuItems);
   }
@@ -117,7 +127,7 @@ EnrichedMarkdownTextInputProps::EnrichedMarkdownTextInputProps(
     isOnChangeMarkdownSet(convertRawProp(context, rawProps, "isOnChangeMarkdownSet", sourceProps.isOnChangeMarkdownSet, {false})),
     contextMenuItems(convertRawProp(context, rawProps, "contextMenuItems", sourceProps.contextMenuItems, {})),
     linkRegex(convertRawProp(context, rawProps, "linkRegex", sourceProps.linkRegex, {})) {}
-
+
 #ifdef RN_SERIALIZABLE_STATE
 ComponentName EnrichedMarkdownTextInputProps::getDiffPropsImplementationTarget() const {
   return "EnrichedMarkdownTextInput";
@@ -133,79 +143,79 @@ folly::dynamic EnrichedMarkdownTextInputProps::getDiffProps(
     return folly::dynamic::object();
   }
   folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
-
+
   if (defaultValue != oldProps->defaultValue) {
     result["defaultValue"] = defaultValue;
   }
-
+
   if (placeholder != oldProps->placeholder) {
     result["placeholder"] = placeholder;
   }
-
+
   if (placeholderTextColor != oldProps->placeholderTextColor) {
     result["placeholderTextColor"] = *placeholderTextColor;
   }
-
+
   if (editable != oldProps->editable) {
     result["editable"] = editable;
   }
-
+
   if (autoFocus != oldProps->autoFocus) {
     result["autoFocus"] = autoFocus;
   }
-
+
   if (scrollEnabled != oldProps->scrollEnabled) {
     result["scrollEnabled"] = scrollEnabled;
   }
-
+
   if (autoCapitalize != oldProps->autoCapitalize) {
     result["autoCapitalize"] = autoCapitalize;
   }
-
+
   if (multiline != oldProps->multiline) {
     result["multiline"] = multiline;
   }
-
+
   if (cursorColor != oldProps->cursorColor) {
     result["cursorColor"] = *cursorColor;
   }
-
+
   if (selectionColor != oldProps->selectionColor) {
     result["selectionColor"] = *selectionColor;
   }
-
+
   if (markdownStyle != oldProps->markdownStyle) {
     result["markdownStyle"] = toDynamic(markdownStyle);
   }
-
+
   if (color != oldProps->color) {
     result["color"] = *color;
   }
-
+
   if ((fontSize != oldProps->fontSize) && !(std::isnan(fontSize) && std::isnan(oldProps->fontSize))) {
     result["fontSize"] = fontSize;
   }
-
+
   if ((lineHeight != oldProps->lineHeight) && !(std::isnan(lineHeight) && std::isnan(oldProps->lineHeight))) {
     result["lineHeight"] = lineHeight;
   }
-
+
   if (fontFamily != oldProps->fontFamily) {
     result["fontFamily"] = fontFamily;
   }
-
+
   if (fontWeight != oldProps->fontWeight) {
     result["fontWeight"] = fontWeight;
   }
-
+
   if (isOnChangeMarkdownSet != oldProps->isOnChangeMarkdownSet) {
     result["isOnChangeMarkdownSet"] = isOnChangeMarkdownSet;
   }
-
+
   if (contextMenuItems != oldProps->contextMenuItems) {
     result["contextMenuItems"] = toDynamic(contextMenuItems);
   }
-
+
   if (linkRegex != oldProps->linkRegex) {
     result["linkRegex"] = toDynamic(linkRegex);
   }
@@ -218,6 +228,8 @@ EnrichedMarkdownTextProps::EnrichedMarkdownTextProps(
     const RawProps &rawProps): ViewProps(context, sourceProps, rawProps),

     markdown(convertRawProp(context, rawProps, "markdown", sourceProps.markdown, {})),
+    nativeMarkdownBlockId(convertRawProp(context, rawProps, "nativeMarkdownBlockId", sourceProps.nativeMarkdownBlockId, {})),
+    nativeMarkdownRevision(convertRawProp(context, rawProps, "nativeMarkdownRevision", sourceProps.nativeMarkdownRevision, {0.0})),
     markdownStyle(convertRawProp(context, rawProps, "markdownStyle", sourceProps.markdownStyle, {})),
     enableLinkPreview(convertRawProp(context, rawProps, "enableLinkPreview", sourceProps.enableLinkPreview, {true})),
     selectable(convertRawProp(context, rawProps, "selectable", sourceProps.selectable, {false})),
@@ -228,7 +240,7 @@ EnrichedMarkdownTextProps::EnrichedMarkdownTextProps(
     streamingAnimation(convertRawProp(context, rawProps, "streamingAnimation", sourceProps.streamingAnimation, {false})),
     spoilerOverlay(convertRawProp(context, rawProps, "spoilerOverlay", sourceProps.spoilerOverlay, {std::string{"particles"}})),
     contextMenuItems(convertRawProp(context, rawProps, "contextMenuItems", sourceProps.contextMenuItems, {})) {}
-
+
 #ifdef RN_SERIALIZABLE_STATE
 ComponentName EnrichedMarkdownTextProps::getDiffPropsImplementationTarget() const {
   return "EnrichedMarkdownText";
@@ -244,47 +256,55 @@ folly::dynamic EnrichedMarkdownTextProps::getDiffProps(
     return folly::dynamic::object();
   }
   folly::dynamic result = HostPlatformViewProps::getDiffProps(prevProps);
-
+
   if (markdown != oldProps->markdown) {
     result["markdown"] = markdown;
   }
-
+
+  if (nativeMarkdownBlockId != oldProps->nativeMarkdownBlockId) {
+    result["nativeMarkdownBlockId"] = nativeMarkdownBlockId;
+  }
+
+  if ((nativeMarkdownRevision != oldProps->nativeMarkdownRevision) && !(std::isnan(nativeMarkdownRevision) && std::isnan(oldProps->nativeMarkdownRevision))) {
+    result["nativeMarkdownRevision"] = nativeMarkdownRevision;
+  }
+
   if (markdownStyle != oldProps->markdownStyle) {
     result["markdownStyle"] = toDynamic(markdownStyle);
   }
-
+
   if (enableLinkPreview != oldProps->enableLinkPreview) {
     result["enableLinkPreview"] = enableLinkPreview;
   }
-
+
   if (selectable != oldProps->selectable) {
     result["selectable"] = selectable;
   }
-
+
   if (md4cFlags != oldProps->md4cFlags) {
     result["md4cFlags"] = toDynamic(md4cFlags);
   }
-
+
   if (allowFontScaling != oldProps->allowFontScaling) {
     result["allowFontScaling"] = allowFontScaling;
   }
-
+
   if ((maxFontSizeMultiplier != oldProps->maxFontSizeMultiplier) && !(std::isnan(maxFontSizeMultiplier) && std::isnan(oldProps->maxFontSizeMultiplier))) {
     result["maxFontSizeMultiplier"] = maxFontSizeMultiplier;
   }
-
+
   if (allowTrailingMargin != oldProps->allowTrailingMargin) {
     result["allowTrailingMargin"] = allowTrailingMargin;
   }
-
+
   if (streamingAnimation != oldProps->streamingAnimation) {
     result["streamingAnimation"] = streamingAnimation;
   }
-
+
   if (spoilerOverlay != oldProps->spoilerOverlay) {
     result["spoilerOverlay"] = spoilerOverlay;
   }
-
+
   if (contextMenuItems != oldProps->contextMenuItems) {
     result["contextMenuItems"] = toDynamic(contextMenuItems);
   }
diff --git a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h
index c9e1984..fc4d5b9 100644
--- a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h
+++ b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/Props.h
@@ -1951,6 +1951,8 @@ class EnrichedMarkdownProps final : public ViewProps {
 #pragma mark - Props

   std::string markdown{};
+  std::string nativeMarkdownBlockId{};
+  double nativeMarkdownRevision{0.0};
   EnrichedMarkdownMarkdownStyleStruct markdownStyle{};
   bool enableLinkPreview{true};
   bool selectable{false};
@@ -2120,6 +2122,14 @@ static inline folly::dynamic toDynamic(const EnrichedMarkdownTextInputMarkdownSt
 #endif

 struct EnrichedMarkdownTextInputMarkdownStyleStruct {
+  EnrichedMarkdownMarkdownStyleParagraphStruct paragraph{};
+  EnrichedMarkdownMarkdownStyleH1Struct h1{};
+  EnrichedMarkdownMarkdownStyleH2Struct h2{};
+  EnrichedMarkdownMarkdownStyleH3Struct h3{};
+  EnrichedMarkdownMarkdownStyleH4Struct h4{};
+  EnrichedMarkdownMarkdownStyleH5Struct h5{};
+  EnrichedMarkdownMarkdownStyleH6Struct h6{};
+  EnrichedMarkdownMarkdownStyleCodeBlockStruct codeBlock{};
   EnrichedMarkdownTextInputMarkdownStyleStrongStruct strong{};
   EnrichedMarkdownTextInputMarkdownStyleEmStruct em{};
   EnrichedMarkdownTextInputMarkdownStyleLinkStruct link{};
@@ -2131,6 +2141,14 @@ struct EnrichedMarkdownTextInputMarkdownStyleStruct {

   folly::dynamic toDynamic() const {
     folly::dynamic result = folly::dynamic::object();
+    result["paragraph"] = ::facebook::react::toDynamic(paragraph);
+    result["h1"] = ::facebook::react::toDynamic(h1);
+    result["h2"] = ::facebook::react::toDynamic(h2);
+    result["h3"] = ::facebook::react::toDynamic(h3);
+    result["h4"] = ::facebook::react::toDynamic(h4);
+    result["h5"] = ::facebook::react::toDynamic(h5);
+    result["h6"] = ::facebook::react::toDynamic(h6);
+    result["codeBlock"] = ::facebook::react::toDynamic(codeBlock);
     result["strong"] = ::facebook::react::toDynamic(strong);
     result["em"] = ::facebook::react::toDynamic(em);
     result["link"] = ::facebook::react::toDynamic(link);
@@ -2143,6 +2161,38 @@ struct EnrichedMarkdownTextInputMarkdownStyleStruct {
 static inline void fromRawValue(const PropsParserContext& context, const RawValue &value, EnrichedMarkdownTextInputMarkdownStyleStruct &result) {
   auto map = (std::unordered_map<std::string, RawValue>)value;

+  auto tmp_paragraph = map.find("paragraph");
+  if (tmp_paragraph != map.end()) {
+    fromRawValue(context, tmp_paragraph->second, result.paragraph);
+  }
+  auto tmp_h1 = map.find("h1");
+  if (tmp_h1 != map.end()) {
+    fromRawValue(context, tmp_h1->second, result.h1);
+  }
+  auto tmp_h2 = map.find("h2");
+  if (tmp_h2 != map.end()) {
+    fromRawValue(context, tmp_h2->second, result.h2);
+  }
+  auto tmp_h3 = map.find("h3");
+  if (tmp_h3 != map.end()) {
+    fromRawValue(context, tmp_h3->second, result.h3);
+  }
+  auto tmp_h4 = map.find("h4");
+  if (tmp_h4 != map.end()) {
+    fromRawValue(context, tmp_h4->second, result.h4);
+  }
+  auto tmp_h5 = map.find("h5");
+  if (tmp_h5 != map.end()) {
+    fromRawValue(context, tmp_h5->second, result.h5);
+  }
+  auto tmp_h6 = map.find("h6");
+  if (tmp_h6 != map.end()) {
+    fromRawValue(context, tmp_h6->second, result.h6);
+  }
+  auto tmp_codeBlock = map.find("codeBlock");
+  if (tmp_codeBlock != map.end()) {
+    fromRawValue(context, tmp_codeBlock->second, result.codeBlock);
+  }
   auto tmp_strong = map.find("strong");
   if (tmp_strong != map.end()) {
     fromRawValue(context, tmp_strong->second, result.strong);
@@ -4247,6 +4297,8 @@ class EnrichedMarkdownTextProps final : public ViewProps {
 #pragma mark - Props

   std::string markdown{};
+  std::string nativeMarkdownBlockId{};
+  double nativeMarkdownRevision{0.0};
   EnrichedMarkdownTextMarkdownStyleStruct markdownStyle{};
   bool enableLinkPreview{true};
   bool selectable{false};
diff --git a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/RCTComponentViewHelpers.h b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/RCTComponentViewHelpers.h
index 016b9fb..c6094a1 100644
--- a/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/RCTComponentViewHelpers.h
+++ b/ios/generated/ReactCodegen/EnrichedMarkdownTextSpec/RCTComponentViewHelpers.h
@@ -22,6 +22,7 @@ NS_ASSUME_NONNULL_BEGIN
 - (void)blur;
 - (void)setValue:(NSString *)markdown;
 - (void)setSelection:(NSInteger)start end:(NSInteger)end;
+- (void)setSelectionForVerticalNavigation:(NSString *)direction preferredX:(double)preferredX;
 - (void)toggleBold;
 - (void)toggleItalic;
 - (void)toggleUnderline;
@@ -115,6 +116,34 @@ NSObject *arg1 = args[1];
   return;
 }

+if ([commandName isEqualToString:@"setSelectionForVerticalNavigation"]) {
+#if RCT_DEBUG
+  if ([args count] != 2) {
+    RCTLogError(@"%@ command %@ received %d arguments, expected %d.", @"EnrichedMarkdownTextInput", commandName, (int)[args count], 2);
+    return;
+  }
+#endif
+
+  NSObject *arg0 = args[0];
+#if RCT_DEBUG
+  if (!RCTValidateTypeOfViewCommandArgument(arg0, [NSString class], @"string", @"EnrichedMarkdownTextInput", commandName, @"1st")) {
+    return;
+  }
+#endif
+  NSString * direction = (NSString *)arg0;
+
+NSObject *arg1 = args[1];
+#if RCT_DEBUG
+  if (!RCTValidateTypeOfViewCommandArgument(arg1, [NSNumber class], @"number", @"EnrichedMarkdownTextInput", commandName, @"2nd")) {
+    return;
+  }
+#endif
+  double preferredX = [(NSNumber *)arg1 doubleValue];
+
+  [componentView setSelectionForVerticalNavigation:direction preferredX:preferredX];
+  return;
+}
+
 if ([commandName isEqualToString:@"toggleBold"]) {
 #if RCT_DEBUG
   if ([args count] != 0) {
diff --git a/ios/input/ENRMInputFormatter.h b/ios/input/ENRMInputFormatter.h
index dfd6c3f..eb09489 100644
--- a/ios/input/ENRMInputFormatter.h
+++ b/ios/input/ENRMInputFormatter.h
@@ -12,6 +12,7 @@ NS_ASSUME_NONNULL_BEGIN
 /// Base text properties
 @property (nonatomic, strong) UIFont *baseFont;
 @property (nonatomic, strong) RCTUIColor *baseTextColor;
+@property (nonatomic, assign) CGFloat baseLineHeight;

 /// Bold — color override (nil = inherit baseTextColor)
 @property (nonatomic, strong, nullable) RCTUIColor *boldColor;
diff --git a/ios/input/ENRMInputFormatter.mm b/ios/input/ENRMInputFormatter.mm
index b81743b..f211ab0 100644
--- a/ios/input/ENRMInputFormatter.mm
+++ b/ios/input/ENRMInputFormatter.mm
@@ -17,6 +17,7 @@ - (instancetype)init
   if (self = [super init]) {
     _baseFont = [UIFont systemFontOfSize:16.0];
     _baseTextColor = [RCTUIColor labelColor];
+    _baseLineHeight = 0;
     _fontCache = [NSMutableDictionary dictionary];
   }
   return self;
@@ -27,6 +28,7 @@ - (id)copyWithZone:(NSZone *)zone
   ENRMInputFormatterStyle *copy = [[ENRMInputFormatterStyle allocWithZone:zone] init];
   copy.baseFont = _baseFont;
   copy.baseTextColor = _baseTextColor;
+  copy.baseLineHeight = _baseLineHeight;
   copy.boldColor = _boldColor;
   copy.italicColor = _italicColor;
   copy.linkColor = _linkColor;
@@ -119,6 +121,14 @@ - (void)applyFormattingRanges:(NSArray<ENRMFormattingRange *> *)ranges

   [textStorage addAttribute:NSFontAttributeName value:style.baseFont range:fullTextRange];
   [textStorage addAttribute:NSForegroundColorAttributeName value:style.baseTextColor range:fullTextRange];
+  if (style.baseLineHeight > 0) {
+    NSMutableParagraphStyle *paragraphStyle = [[NSMutableParagraphStyle alloc] init];
+    paragraphStyle.minimumLineHeight = style.baseLineHeight;
+    paragraphStyle.maximumLineHeight = style.baseLineHeight;
+    [textStorage addAttribute:NSParagraphStyleAttributeName value:paragraphStyle range:fullTextRange];
+  } else {
+    [textStorage removeAttribute:NSParagraphStyleAttributeName range:fullTextRange];
+  }
   [textStorage removeAttribute:NSUnderlineStyleAttributeName range:fullTextRange];
   [textStorage removeAttribute:NSStrikethroughStyleAttributeName range:fullTextRange];
   [textStorage removeAttribute:NSBackgroundColorAttributeName range:fullTextRange];
diff --git a/ios/input/ENRMInputTextView.mm b/ios/input/ENRMInputTextView.mm
index 187cf5b..340d25a 100644
--- a/ios/input/ENRMInputTextView.mm
+++ b/ios/input/ENRMInputTextView.mm
@@ -83,6 +83,12 @@ - (void)layoutSubviews

 #else // TARGET_OS_OSX

+@interface ENRMInputTextView ()
+@property (nonatomic, strong) NSTimer *enrmSelectionDragTimer;
+@property (nonatomic, assign) BOOL enrmSelectionDragOutsideEmitted;
+@property (nonatomic, assign) NSPoint enrmLastSelectionDragWindowPoint;
+@end
+
 @implementation ENRMInputTextView

 - (void)copy:(id)sender
@@ -147,14 +153,185 @@ - (BOOL)acceptsFirstResponder
   return self.isEditable;
 }

+- (BOOL)enrmCanNavigateVerticallyOutside
+{
+  return self.markdownTextInput != nil && self.selectedRange.length == 0 && self.layoutManager != nil && self.textContainer != nil;
+}
+
+- (NSUInteger)enrmGlyphIndexForSelection
+{
+  NSLayoutManager *layoutManager = self.layoutManager;
+  NSTextContainer *textContainer = self.textContainer;
+  if (layoutManager == nil || textContainer == nil) {
+    return NSNotFound;
+  }
+
+  [layoutManager ensureLayoutForTextContainer:textContainer];
+  NSUInteger glyphCount = layoutManager.numberOfGlyphs;
+  if (glyphCount == 0) {
+    return NSNotFound;
+  }
+
+  NSUInteger textLength = self.string.length;
+  NSUInteger characterIndex = MIN(self.selectedRange.location, textLength);
+  NSUInteger glyphIndex = characterIndex >= textLength
+    ? glyphCount - 1
+    : [layoutManager glyphIndexForCharacterAtIndex:characterIndex];
+  return MIN(glyphIndex, glyphCount - 1);
+}
+
+- (BOOL)enrmSelectionIsOnFirstVisualLine
+{
+  NSUInteger glyphIndex = [self enrmGlyphIndexForSelection];
+  if (glyphIndex == NSNotFound) {
+    return YES;
+  }
+
+  NSRange lineRange = NSMakeRange(0, 0);
+  [self.layoutManager lineFragmentRectForGlyphAtIndex:glyphIndex effectiveRange:&lineRange];
+  return lineRange.location == 0;
+}
+
+- (BOOL)enrmSelectionIsOnLastVisualLine
+{
+  NSLayoutManager *layoutManager = self.layoutManager;
+  NSTextContainer *textContainer = self.textContainer;
+  if (layoutManager == nil || textContainer == nil) {
+    return YES;
+  }
+
+  [layoutManager ensureLayoutForTextContainer:textContainer];
+  NSUInteger glyphCount = layoutManager.numberOfGlyphs;
+  NSUInteger glyphIndex = [self enrmGlyphIndexForSelection];
+  if (glyphIndex == NSNotFound) {
+    return YES;
+  }
+
+  NSRange lineRange = NSMakeRange(0, 0);
+  [layoutManager lineFragmentRectForGlyphAtIndex:glyphIndex effectiveRange:&lineRange];
+  return NSMaxRange(lineRange) >= glyphCount;
+}
+
+- (CGFloat)enrmPreferredXForSelection
+{
+  if (self.window != nil) {
+    NSRange selectedRange = self.selectedRange;
+    NSRect screenRect = [self firstRectForCharacterRange:NSMakeRange(selectedRange.location, 0) actualRange:NULL];
+    if (!NSEqualRects(screenRect, NSZeroRect)) {
+      NSRect windowRect = [self.window convertRectFromScreen:screenRect];
+      NSRect localRect = [self convertRect:windowRect fromView:nil];
+      return NSMinX(localRect);
+    }
+  }
+
+  return self.textContainerInset.left;
+}
+
+- (BOOL)enrmEmitVerticalNavigationOutsideIfNeeded:(NSString *)direction
+{
+  BOOL isAtEdge = [direction isEqualToString:@"up"]
+    ? [self enrmSelectionIsOnFirstVisualLine]
+    : [self enrmSelectionIsOnLastVisualLine];
+
+  if (isAtEdge) {
+    [self.markdownTextInput emitVerticalNavigationOutsideWithDirection:direction preferredX:[self enrmPreferredXForSelection]];
+  }
+
+  return isAtEdge;
+}
+
+- (void)moveUp:(id)sender
+{
+  if ([self enrmCanNavigateVerticallyOutside] && [self enrmEmitVerticalNavigationOutsideIfNeeded:@"up"]) {
+    return;
+  }
+
+  [super moveUp:sender];
+}
+
+- (void)moveDown:(id)sender
+{
+  if ([self enrmCanNavigateVerticallyOutside] && [self enrmEmitVerticalNavigationOutsideIfNeeded:@"down"]) {
+    return;
+  }
+
+  [super moveDown:sender];
+}
+
 - (void)mouseDown:(NSEvent *)event
 {
   if (self.window != nil) {
     [self.window makeFirstResponder:self];
   }
+  [self enrmStartSelectionDragPolling];
   [super mouseDown:event];
 }

+- (CGFloat)enrmTopOriginWindowYForWindowPoint:(NSPoint)windowPoint
+{
+  NSView *contentView = self.window.contentView;
+  if (contentView == nil) {
+    return windowPoint.y;
+  }
+  return NSHeight(contentView.bounds) - windowPoint.y;
+}
+
+- (void)enrmStartSelectionDragPolling
+{
+  [self enrmStopSelectionDragPolling];
+  self.enrmSelectionDragOutsideEmitted = NO;
+  self.enrmLastSelectionDragWindowPoint = self.window.mouseLocationOutsideOfEventStream;
+  __weak ENRMInputTextView *weakSelf = self;
+  self.enrmSelectionDragTimer = [NSTimer timerWithTimeInterval:0.016
+                                                       repeats:YES
+                                                         block:^(__unused NSTimer *timer) {
+                                                           [weakSelf enrmPollSelectionDrag];
+                                                         }];
+  [[NSRunLoop currentRunLoop] addTimer:self.enrmSelectionDragTimer forMode:NSEventTrackingRunLoopMode];
+  [[NSRunLoop currentRunLoop] addTimer:self.enrmSelectionDragTimer forMode:NSRunLoopCommonModes];
+}
+
+- (void)enrmStopSelectionDragPolling
+{
+  [self.enrmSelectionDragTimer invalidate];
+  self.enrmSelectionDragTimer = nil;
+}
+
+- (void)enrmPollSelectionDrag
+{
+  if ((NSEvent.pressedMouseButtons & 1) == 0 || self.window == nil) {
+    if (self.enrmSelectionDragOutsideEmitted && self.markdownTextInput != nil) {
+      CGFloat windowX = self.enrmLastSelectionDragWindowPoint.x;
+      CGFloat windowY = [self enrmTopOriginWindowYForWindowPoint:self.enrmLastSelectionDragWindowPoint];
+      [self.markdownTextInput emitSelectionDragOutsideWithDirection:@"end" windowX:windowX windowY:windowY];
+    }
+    [self enrmStopSelectionDragPolling];
+    return;
+  }
+
+  [self enrmHandleSelectionDragAtWindowPoint:self.window.mouseLocationOutsideOfEventStream];
+}
+
+- (void)enrmHandleSelectionDragAtWindowPoint:(NSPoint)windowPoint
+{
+  self.enrmLastSelectionDragWindowPoint = windowPoint;
+  if (self.markdownTextInput == nil || self.selectedRange.length == 0) {
+    return;
+  }
+
+  NSRect boundsInWindow = [self convertRect:self.bounds toView:nil];
+  CGFloat pointerY = windowPoint.y;
+  CGFloat windowX = windowPoint.x;
+  CGFloat windowY = [self enrmTopOriginWindowYForWindowPoint:windowPoint];
+  if (pointerY < NSMinY(boundsInWindow)) {
+    self.enrmSelectionDragOutsideEmitted = YES;
+    [self.markdownTextInput emitSelectionDragOutsideWithDirection:@"down" windowX:windowX windowY:windowY];
+  } else if (pointerY > NSMaxY(boundsInWindow)) {
+    self.enrmSelectionDragOutsideEmitted = YES;
+    [self.markdownTextInput emitSelectionDragOutsideWithDirection:@"up" windowX:windowX windowY:windowY];
+  }
+}
+
 - (NSMenu *)menuForEvent:(NSEvent *)event
 {
   NSMenu *menu = [super menuForEvent:event];
diff --git a/ios/input/EnrichedMarkdownTextInput+Internal.h b/ios/input/EnrichedMarkdownTextInput+Internal.h
index f02ae61..ce8ce1b 100644
--- a/ios/input/EnrichedMarkdownTextInput+Internal.h
+++ b/ios/input/EnrichedMarkdownTextInput+Internal.h
@@ -23,6 +23,8 @@ NS_ASSUME_NONNULL_BEGIN

 #if TARGET_OS_OSX
 - (NSMenu *)enrichedMenuForEvent:(NSEvent *)event defaultMenu:(NSMenu *)menu textView:(NSTextView *)textView;
+- (void)emitSelectionDragOutsideWithDirection:(NSString *)direction windowX:(CGFloat)windowX windowY:(CGFloat)windowY;
+- (void)emitVerticalNavigationOutsideWithDirection:(NSString *)direction preferredX:(CGFloat)preferredX;
 #endif

 @end
diff --git a/ios/input/EnrichedMarkdownTextInput.mm b/ios/input/EnrichedMarkdownTextInput.mm
index 96969e8..ec23be1 100644
--- a/ios/input/EnrichedMarkdownTextInput.mm
+++ b/ios/input/EnrichedMarkdownTextInput.mm
@@ -38,6 +38,13 @@ @interface EnrichedMarkdownTextInput () <RCTEnrichedMarkdownTextInputViewProtoco
 #endif
 - (void)setupTextView;
 - (void)applyFormatting;
+- (void)applyBlockStyleForMarkdown:(NSString *)markdown;
+- (NSString *)blockStyleMarkdownForMarkdown:(NSString *)markdown;
+- (NSRange)selectedRange;
+- (void)setBlockStyleMarkdown:(NSString *)markdown;
+- (void)setHangingMarkdownPrefixLength:(NSInteger)prefixLength;
+- (void)setSelectionForWindowPointX:(CGFloat)x y:(CGFloat)y;
 - (void)toggleInlineStyle:(ENRMInputStyleType)styleType;
 - (void)resetBaseTypingAttributes;
 - (void)replaceSelectedTextWith:(NSString *)text formattingRanges:(NSArray<ENRMFormattingRange *> *)ranges;
@@ -70,7 +77,11 @@ @implementation EnrichedMarkdownTextInput {
   std::optional<CGRect> _prevCaretRect;

 #if TARGET_OS_OSX
+  NSView *_scrollContainerView;
   NSScrollView *_scrollView;
+  NSString *_blockStyleMarkdownOverride;
+  NSInteger _hangingPrefixLength;
 #endif

   NSArray<NSString *> *_contextMenuItemTexts;
@@ -161,6 +172,9 @@ - (void)setupTextView
 #else
   _textView.selectable = YES;

+  _scrollContainerView = [[NSView alloc] initWithFrame:CGRectZero];
+  _scrollContainerView.autoresizingMask = NSViewWidthSizable | NSViewHeightSizable;
+
   _scrollView = [[NSScrollView alloc] initWithFrame:CGRectZero];
   _scrollView.backgroundColor = [RCTUIColor clearColor];
   _scrollView.drawsBackground = NO;
@@ -175,7 +189,8 @@ - (void)setupTextView
   _textView.textContainer.widthTracksTextView = YES;

   _scrollView.documentView = _textView;
-  self.contentView = _scrollView;
+  [_scrollContainerView addSubview:_scrollView];
+  self.contentView = _scrollContainerView;
 #endif

   _placeholderLabel = ENRMCreatePlaceholderLabel(_textView, _formatterStyle.baseFont);
@@ -211,6 +226,122 @@ - (void)requestHeightUpdate

 #pragma mark - Measurement

+#if TARGET_OS_OSX
+- (void)applyHangingPrefixParagraphStyle
+{
+  NSUInteger textLength = _textView.textStorage.length;
+  if (textLength == 0) {
+    return;
+  }
+
+  NSRange range = NSMakeRange(0, textLength);
+  [_textView.textStorage beginEditing];
+  NSParagraphStyle *existingStyle = [_textView.textStorage attribute:NSParagraphStyleAttributeName
+                                                             atIndex:0
+                                                      effectiveRange:NULL];
+  NSMutableParagraphStyle *paragraphStyle = existingStyle != nil
+    ? [existingStyle mutableCopy]
+    : [[NSMutableParagraphStyle alloc] init];
+  paragraphStyle.firstLineHeadIndent = 0;
+  paragraphStyle.headIndent = 0;
+  [_textView.textStorage addAttribute:NSParagraphStyleAttributeName value:paragraphStyle range:range];
+  if (_hangingPrefixLength > 0) {
+    NSUInteger prefixLength = MIN((NSUInteger)_hangingPrefixLength, textLength);
+    [_textView.textStorage addAttribute:NSForegroundColorAttributeName
+                                  value:NSColor.clearColor
+                                  range:NSMakeRange(0, prefixLength)];
+    [_textView.textStorage addAttribute:NSFontAttributeName
+                                  value:[NSFont systemFontOfSize:0.1]
+                                  range:NSMakeRange(0, prefixLength)];
+  }
+  [_textView.textStorage endEditing];
+}
+
+- (void)applyTextViewLayout
+{
+  if (_scrollContainerView == nil || _scrollView == nil || _textView == nil) {
+    return;
+  }
+
+  NSRect bounds = self.bounds;
+  CGFloat width = MAX(0, NSWidth(bounds));
+  CGFloat height = MAX(0, NSHeight(bounds));
+  _scrollContainerView.frame = bounds;
+  _scrollView.frame = NSMakeRect(0, 0, width, height);
+
+  NSRect textFrame = _textView.frame;
+  textFrame.origin.x = 0;
+  textFrame.origin.y = 0;
+  textFrame.size.width = width;
+  textFrame.size.height = height;
+  _textView.frame = textFrame;
+  _textView.textContainer.size = CGSizeMake(width, CGFLOAT_MAX);
+  ENRMRefreshTextViewLayout(_textView);
+}
+
+- (NSInteger)hangingMarkdownPrefixLengthForMarkdown:(NSString *)markdown
+{
+  NSInteger prefixLength = 0;
+
+  if (markdown.length > 0) {
+    static NSRegularExpression *headingPrefixRegex = nil;
+    static dispatch_once_t onceToken;
+    dispatch_once(&onceToken, ^{
+      headingPrefixRegex = [NSRegularExpression regularExpressionWithPattern:@"^[ \\t]{0,3}#{1,6}[ \\t]+"
+                                                                      options:0
+                                                                        error:nil];
+    });
+
+    NSTextCheckingResult *match = [headingPrefixRegex firstMatchInString:markdown
+                                                                 options:0
+                                                                   range:NSMakeRange(0, markdown.length)];
+    if (match != nil) {
+      prefixLength = (NSInteger)NSMaxRange(match.range);
+    }
+  }
+
+  return prefixLength;
+}
+
+- (void)refreshHangingMarkdownPrefixForMarkdown:(NSString *)markdown
+{
+  [self setHangingMarkdownPrefixLength:[self hangingMarkdownPrefixLengthForMarkdown:markdown]];
+}
+
+- (void)setHangingMarkdownPrefixLength:(NSInteger)prefixLength
+{
+  NSInteger clampedPrefixLength = MAX(0, prefixLength);
+  if (_hangingPrefixLength == clampedPrefixLength) {
+    return;
+  }
+
+  _hangingPrefixLength = clampedPrefixLength;
+  [self applyHangingPrefixParagraphStyle];
+  [self requestHeightUpdate];
+}
+
+- (void)layout
+{
+  [super layout];
+  [self applyTextViewLayout];
+}
+#else
+- (void)setHangingMarkdownPrefixLength:(NSInteger)prefixLength
+{
+}
+#endif
+
 - (CGSize)measureSize:(CGFloat)maxWidth
 {
   NSMutableAttributedString *measuredText =
@@ -233,8 +364,14 @@ - (CGSize)measureSize:(CGFloat)maxWidth
     }
   }

+  CGFloat measurementWidth = maxWidth;
+
   CGRect boundingBox =
-      [measuredText boundingRectWithSize:CGSizeMake(maxWidth, CGFLOAT_MAX)
+      [measuredText boundingRectWithSize:CGSizeMake(measurementWidth, CGFLOAT_MAX)
                                  options:NSStringDrawingUsesLineFragmentOrigin | NSStringDrawingUsesFontLeading
                                  context:nil];

@@ -331,7 +468,7 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &

     [self resetBaseTypingAttributes];

-    if (_formattingStore.allRanges.count > 0) {
+    if (_textView.textStorage.length > 0) {
       [self applyFormatting];
     }

@@ -346,7 +483,7 @@ - (void)updateProps:(Props::Shared const &)props oldProps:(Props::Shared const &
 - (void)scheduleRelayoutIfNeeded
 {
   [NSObject cancelPreviousPerformRequestsWithTarget:self selector:@selector(_performRelayout) object:nil];
-  [self performSelector:@selector(_performRelayout) withObject:nil afterDelay:0];
+  [self _performRelayout];
 }

 - (void)_performRelayout
@@ -355,20 +492,30 @@ - (void)_performRelayout
     return;
   }

-  dispatch_async(dispatch_get_main_queue(), ^{
+  void (^relayout)(void) = ^{
     NSUInteger textLength = self->_textView.textStorage.length;
-    if (textLength == 0) {
-      return;
+    if (textLength > 0) {
+      NSRange wholeRange = NSMakeRange(0, textLength);
+      NSRange actualRange = NSMakeRange(0, 0);
+      [self->_textView.layoutManager invalidateLayoutForCharacterRange:wholeRange actualCharacterRange:&actualRange];
+      [self->_textView.layoutManager ensureLayoutForCharacterRange:actualRange];
+      [self->_textView.layoutManager invalidateDisplayForCharacterRange:wholeRange];
+
+      CGFloat measurementWidth = self.bounds.size.width;
+      measurementWidth = measurementWidth > 0 ? measurementWidth : self->_textView.frame.size.width;
+      CGSize measuredSize = [self measureSize:measurementWidth];
+      ENRMSetContentSize(self->_textView, measuredSize);
+      [[NSNotificationCenter defaultCenter] postNotificationName:@"ENRMMarkdownTextInputContentSizeDidChangeNotification" object:self];
     }
-    NSRange wholeRange = NSMakeRange(0, textLength);
-    NSRange actualRange = NSMakeRange(0, 0);
-    [self->_textView.layoutManager invalidateLayoutForCharacterRange:wholeRange actualCharacterRange:&actualRange];
-    [self->_textView.layoutManager ensureLayoutForCharacterRange:actualRange];
-    [self->_textView.layoutManager invalidateDisplayForCharacterRange:wholeRange];
-
-    CGSize measuredSize = [self measureSize:self->_textView.frame.size.width];
-    ENRMSetContentSize(self->_textView, measuredSize);
-  });
+  };
+
+  if ([NSThread isMainThread]) {
+    relayout();
+  } else {
+    dispatch_async(dispatch_get_main_queue(), relayout);
+  }
 }

 #pragma mark - Window attachment
@@ -379,6 +526,9 @@ - (void)didMoveToWindow

   if (self.window) {
     // Don't override the contentView frame set by RCTViewComponentView.
+#if TARGET_OS_OSX
+    [self applyTextViewLayout];
+#endif
     ENRMRefreshTextViewLayout(_textView);

     [self applyFormatting];
@@ -453,6 +603,7 @@ - (void)updatePlaceholderVisibility

 - (void)importMarkdown:(NSString *)markdown
 {
   ENRMInputParser *parser = [[ENRMInputParser alloc] init];
   ENRMParseResult *parsed = [parser parseToPlainTextAndRanges:markdown];
+  NSString *blockStyleMarkdown = [self blockStyleMarkdownForMarkdown:markdown];

@@ -466,6 +617,10 @@ - (void)importMarkdown:(NSString *)markdown
   _lastTextLength = parsed.plainText.length;
   _lastSelectedRange = _textView.selectedRange;
+  [self applyBlockStyleForMarkdown:blockStyleMarkdown];
   [self applyFormatting];
+#if TARGET_OS_OSX
+  [self refreshHangingMarkdownPrefixForMarkdown:markdown ?: @""];
+#endif
   [self updatePlaceholderVisibility];

   _blockEmitting = NO;
@@ -512,12 +667,93 @@ - (void)pasteMarkdown:(NSString *)markdown

 #pragma mark - Formatting

+- (NSString *)blockStyleMarkdownForMarkdown:(NSString *)markdown
+{
+#if TARGET_OS_OSX
+  return _blockStyleMarkdownOverride ?: markdown ?: @"";
+#else
+  return markdown ?: @"";
+#endif
+}
+
+- (void)setBlockStyleMarkdown:(NSString *)markdown
+{
+#if TARGET_OS_OSX
+  _blockStyleMarkdownOverride = [markdown copy];
+  [self applyBlockStyleForMarkdown:[self blockStyleMarkdownForMarkdown:markdown]];
+  [self applyFormatting];
+  [self requestHeightUpdate];
+#endif
+}
+
+- (NSRange)selectedRange
+{
+  return _textView.selectedRange;
+}
+
 - (void)resetBaseTypingAttributes
 {
-  ENRMSetDefaultTypingAttributes(_textView, @{
+  _textView.font = _formatterStyle.baseFont;
+  NSMutableDictionary *attrs = [@{
     NSFontAttributeName : _formatterStyle.baseFont,
     NSForegroundColorAttributeName : _formatterStyle.baseTextColor,
-  });
+  } mutableCopy];
+  if (_formatterStyle.baseLineHeight > 0) {
+    NSMutableParagraphStyle *paragraphStyle = [[NSMutableParagraphStyle alloc] init];
+    paragraphStyle.minimumLineHeight = _formatterStyle.baseLineHeight;
+    paragraphStyle.maximumLineHeight = _formatterStyle.baseLineHeight;
+    attrs[NSParagraphStyleAttributeName] = paragraphStyle;
+  }
+  ENRMSetDefaultTypingAttributes(_textView, attrs);
+}
+
+- (void)applyBlockStyleForMarkdown:(NSString *)markdown
+{
+  const auto &viewProps = *std::static_pointer_cast<EnrichedMarkdownTextInputProps const>(_props);
+  NSString *trimmedMarkdown = [markdown stringByTrimmingCharactersInSet:NSCharacterSet.whitespaceAndNewlineCharacterSet];
+  BOOL changed = NO;
+
+  if ([trimmedMarkdown hasPrefix:@"```"] || [trimmedMarkdown hasPrefix:@"~~~"]) {
+    changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.codeBlock);
+  } else {
+    NSRegularExpression *headingRegex = [NSRegularExpression regularExpressionWithPattern:@"^#{1,6}\\s"
+                                                                                  options:0
+                                                                                    error:nil];
+    NSTextCheckingResult *headingMatch = [headingRegex firstMatchInString:trimmedMarkdown
+                                                                  options:0
+                                                                    range:NSMakeRange(0, trimmedMarkdown.length)];
+    if (headingMatch != nil) {
+      NSUInteger headingLevel = headingMatch.range.length - 1;
+      switch (headingLevel) {
+        case 1:
+          changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.h1);
+          break;
+        case 2:
+          changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.h2);
+          break;
+        case 3:
+          changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.h3);
+          break;
+        case 4:
+          changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.h4);
+          break;
+        case 5:
+          changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.h5);
+          break;
+        default:
+          changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.h6);
+          break;
+      }
+    } else {
+      changed = applyInputBlockStyleProps(_formatterStyle, viewProps.markdownStyle.paragraph);
+    }
+  }
+
+  if (changed) {
+    [_formatterStyle invalidateFontCache];
+    _placeholderLabel.font = _formatterStyle.baseFont;
+    [self resetBaseTypingAttributes];
+  }
 }

 - (void)applyFormatting
@@ -534,6 +770,9 @@ - (void)applyFormatting

   [_formatter applyFormattingRanges:_formattingStore.allRanges toTextView:_textView style:_formatterStyle];
   [_detectorPipeline refreshAllStyling];
+#if TARGET_OS_OSX
+  [self applyHangingPrefixParagraphStyle];
+#endif

   NSUInteger textLen = ENRMGetPlainText(_textView).length;
   if (savedSelection.location + savedSelection.length <= textLen) {
@@ -565,5 +804,22 @@ - (void)setValue:(NSString *)markdown
   [self requestHeightUpdate];
 }
+- (void)setValuePreservingSelection:(NSString *)markdown
+{
+  NSRange selection = _textView.selectedRange;
+  [self importMarkdown:markdown];
+
+  NSUInteger textLength = ENRMGetPlainText(_textView).length;
+  NSUInteger selectionStart = MIN(selection.location, textLength);
+  NSUInteger selectionEnd = MIN(NSMaxRange(selection), textLength);
+  _textView.selectedRange = NSMakeRange(selectionStart, selectionEnd - selectionStart);
+  _lastSelectedRange = _textView.selectedRange;
+
+  [self emitOnChangeText];
+  [self emitOnChangeSelection];
+  [self emitOnChangeState];
+  [self requestHeightUpdate];
+}
+
 - (void)setSelection:(NSInteger)start end:(NSInteger)end
 {
   NSInteger textLen = (NSInteger)ENRMGetPlainText(_textView).length;
@@ -575,6 +831,90 @@ - (void)setSelection:(NSInteger)start end:(NSInteger)end
   [self emitOnChangeState];
 }

+#if TARGET_OS_OSX
+- (NSUInteger)characterIndexForVerticalNavigationDirection:(NSString *)direction preferredX:(CGFloat)preferredX
+{
+  NSString *text = ENRMGetPlainText(_textView);
+  NSTextContainer *textContainer = _textView.textContainer;
+  NSLayoutManager *layoutManager = _textView.layoutManager;
+  if (text.length == 0 || textContainer == nil || layoutManager == nil) {
+    return 0;
+  }
+
+  [layoutManager ensureLayoutForTextContainer:textContainer];
+  NSUInteger glyphCount = layoutManager.numberOfGlyphs;
+  if (glyphCount == 0) {
+    return 0;
+  }
+
+  NSUInteger edgeGlyphIndex = [direction isEqualToString:@"up"] ? glyphCount - 1 : 0;
+  NSRange lineRange = NSMakeRange(0, 0);
+  NSRect lineRect = [layoutManager lineFragmentRectForGlyphAtIndex:edgeGlyphIndex effectiveRange:&lineRange];
+  CGFloat textContainerX = preferredX - _textView.textContainerInset.left;
+  NSPoint targetPoint = NSMakePoint(textContainerX, NSMidY(lineRect));
+  CGFloat fraction = 0;
+  NSUInteger glyphIndex = [layoutManager glyphIndexForPoint:targetPoint inTextContainer:textContainer fractionOfDistanceThroughGlyph:&fraction];
+  glyphIndex = MIN(MAX(glyphIndex, lineRange.location), NSMaxRange(lineRange) - 1);
+
+  NSUInteger characterIndex = [layoutManager characterIndexForGlyphAtIndex:glyphIndex];
+  if (fraction > 0.5) {
+    characterIndex += 1;
+  }
+
+  return MIN(characterIndex, text.length);
+}
+
+- (void)setSelectionForVerticalNavigation:(NSString *)direction preferredX:(double)preferredX
+{
+  NSUInteger characterIndex = [self characterIndexForVerticalNavigationDirection:direction preferredX:(CGFloat)preferredX];
+  _textView.selectedRange = NSMakeRange(characterIndex, 0);
+  [_textView scrollRangeToVisible:_textView.selectedRange];
+  [self emitOnChangeSelection];
+  [self emitOnChangeState];
+}
+#else
+- (void)setSelectionForVerticalNavigation:(NSString *)direction preferredX:(double)preferredX
+{
+  NSInteger textLen = (NSInteger)ENRMGetPlainText(_textView).length;
+  NSInteger selection = [direction isEqualToString:@"up"] ? textLen : 0;
+  [self setSelection:selection end:selection];
+}
+#endif
+
+- (void)setSelectionForWindowPointX:(CGFloat)x y:(CGFloat)y
+{
+#if TARGET_OS_OSX
+  NSUInteger textLength = ENRMGetPlainText(_textView).length;
+  NSTextContainer *textContainer = _textView.textContainer;
+  NSLayoutManager *layoutManager = _textView.layoutManager;
+  NSUInteger characterIndex = 0;
+
+  if (textLength > 0 && textContainer != nil && layoutManager != nil) {
+    [layoutManager ensureLayoutForTextContainer:textContainer];
+    if (layoutManager.numberOfGlyphs > 0) {
+      NSPoint localPoint = [_textView convertPoint:NSMakePoint(x, y) fromView:nil];
+      NSPoint textPoint = NSMakePoint(localPoint.x - _textView.textContainerInset.left, localPoint.y - _textView.textContainerInset.top);
+      CGFloat fraction = 0;
+      NSUInteger glyphIndex = [layoutManager glyphIndexForPoint:textPoint
+                                                inTextContainer:textContainer
+                               fractionOfDistanceThroughGlyph:&fraction];
+      glyphIndex = MIN(glyphIndex, layoutManager.numberOfGlyphs - 1);
+      characterIndex = [layoutManager characterIndexForGlyphAtIndex:glyphIndex];
+      if (fraction > 0.5) {
+        characterIndex += 1;
+      }
+      characterIndex = MIN(characterIndex, textLength);
+    }
+  }
+
+  [self.window makeFirstResponder:_textView];
+  _textView.selectedRange = NSMakeRange(characterIndex, 0);
+  [_textView scrollRangeToVisible:_textView.selectedRange];
+  [self emitOnChangeSelection];
+  [self emitOnChangeState];
+#endif
+}
+
 - (void)toggleBold
 {
   [self toggleInlineStyle:ENRMInputStyleTypeStrong];
@@ -774,11 +1114,22 @@ - (CGRect)computeCaretRect
 #else
   NSRange selection = _textView.selectedRange;
   if (selection.location != NSNotFound) {
-    NSRange glyphRange = [_textView.layoutManager glyphRangeForCharacterRange:NSMakeRange(selection.location, 0)
-                                                         actualCharacterRange:NULL];
-    caretRect = [_textView.layoutManager boundingRectForGlyphRange:glyphRange inTextContainer:_textView.textContainer];
-    caretRect.origin.x += _textView.textContainerInset.left;
-    caretRect.origin.y += _textView.textContainerInset.top;
+    NSUInteger selectionLocation = MIN(selection.location, _textView.string.length);
+    if (_textView.window != nil) {
+      NSRect screenRect = [_textView firstRectForCharacterRange:NSMakeRange(selectionLocation, 0) actualRange:NULL];
+      if (!NSEqualRects(screenRect, NSZeroRect)) {
+        NSRect windowRect = [_textView.window convertRectFromScreen:screenRect];
+        caretRect = [_textView convertRect:windowRect fromView:nil];
+      }
+    }
+
+    if (CGRectEqualToRect(caretRect, CGRectZero)) {
+      NSRange glyphRange = [_textView.layoutManager glyphRangeForCharacterRange:NSMakeRange(selectionLocation, 0)
+                                                           actualCharacterRange:NULL];
+      caretRect = [_textView.layoutManager boundingRectForGlyphRange:glyphRange inTextContainer:_textView.textContainer];
+      caretRect.origin.x += _textView.textContainerInset.left;
+      caretRect.origin.y += _textView.textContainerInset.top;
+    }
   }
 #endif
   return caretRect;
@@ -884,6 +1235,33 @@ - (void)emitOnChangeSelection
   });
 }

+#if TARGET_OS_OSX
+- (void)emitSelectionDragOutsideWithDirection:(NSString *)direction windowX:(CGFloat)windowX windowY:(CGFloat)windowY
+{
+  auto emitter = [self getEventEmitter];
+  if (emitter == nullptr) {
+    return;
+  }
+  emitter->onSelectionDragOutside({
+      .direction = std::string([direction UTF8String] ?: ""),
+      .windowX = windowX,
+      .windowY = windowY,
+  });
+}
+
+- (void)emitVerticalNavigationOutsideWithDirection:(NSString *)direction preferredX:(CGFloat)preferredX
+{
+  auto emitter = [self getEventEmitter];
+  if (emitter == nullptr) {
+    return;
+  }
+  emitter->onVerticalNavigationOutside({
+      .direction = std::string([direction UTF8String] ?: ""),
+      .preferredX = preferredX,
+  });
+}
+#endif
+
 - (void)emitOnChangeState
 {
   auto emitter = [self getEventEmitter];
@@ -914,12 +1292,12 @@ - (void)emitOnChangeState
   _prevState.initialized = YES;

   emitter->onChangeState({
-      .bold = {.isActive = boldActive},
-      .italic = {.isActive = italicActive},
-      .underline = {.isActive = underlineActive},
-      .strikethrough = {.isActive = strikethroughActive},
-      .spoiler = {.isActive = spoilerActive},
-      .link = {.isActive = linkActive},
+      .bold = {.isActive = static_cast<bool>(boldActive)},
+      .italic = {.isActive = static_cast<bool>(italicActive)},
+      .underline = {.isActive = static_cast<bool>(underlineActive)},
+      .strikethrough = {.isActive = static_cast<bool>(strikethroughActive)},
+      .spoiler = {.isActive = static_cast<bool>(spoilerActive)},
+      .link = {.isActive = static_cast<bool>(linkActive)},
   });
 }

@@ -988,12 +1366,12 @@ - (void)emitContextMenuItemPress:(NSString *)itemText
       .selectionEnd = static_cast<int>(NSMaxRange(selectedRange)),
       .styleState =
           {
-              .bold = {.isActive = boldActive},
-              .italic = {.isActive = italicActive},
-              .underline = {.isActive = underlineActive},
-              .strikethrough = {.isActive = strikethroughActive},
-              .spoiler = {.isActive = spoilerActive},
-              .link = {.isActive = linkActive},
+              .bold = {.isActive = static_cast<bool>(boldActive)},
+              .italic = {.isActive = static_cast<bool>(italicActive)},
+              .underline = {.isActive = static_cast<bool>(underlineActive)},
+              .strikethrough = {.isActive = static_cast<bool>(strikethroughActive)},
+              .spoiler = {.isActive = static_cast<bool>(spoilerActive)},
+              .link = {.isActive = static_cast<bool>(linkActive)},
           },
   });
 }
@@ -1090,7 +1468,13 @@ - (void)handleTextChanged
   }
 #endif

+  NSString *markdown = [ENRMMarkdownSerializer serializePlainText:ENRMGetPlainText(_textView)
+                                                           ranges:_formattingStore.allRanges];
+  [self applyBlockStyleForMarkdown:[self blockStyleMarkdownForMarkdown:markdown]];
   [self applyFormatting];
+#if TARGET_OS_OSX
+  [self refreshHangingMarkdownPrefixForMarkdown:markdown ?: @""];
+#endif

   [_detectorPipeline processTextChange:ENRMGetPlainText(_textView)
                      modificationRange:NSMakeRange(editLocation, insertedLength)];
@@ -1149,6 +1533,12 @@ - (void)manageSelectionBasedChanges
         NSMutableDictionary *attrs = [NSMutableDictionary dictionary];
         attrs[NSFontAttributeName] = _formatterStyle.baseFont;
         attrs[NSForegroundColorAttributeName] = _formatterStyle.baseTextColor;
+        if (_formatterStyle.baseLineHeight > 0) {
+          NSMutableParagraphStyle *paragraphStyle = [[NSMutableParagraphStyle alloc] init];
+          paragraphStyle.minimumLineHeight = _formatterStyle.baseLineHeight;
+          paragraphStyle.maximumLineHeight = _formatterStyle.baseLineHeight;
+          attrs[NSParagraphStyleAttributeName] = paragraphStyle;
+        }
         _textView.typingAttributes = attrs;
       }
     }
diff --git a/ios/input/InputStylePropsUtils.h b/ios/input/InputStylePropsUtils.h
index fd76ab3..064aa2c 100644
--- a/ios/input/InputStylePropsUtils.h
+++ b/ios/input/InputStylePropsUtils.h
@@ -63,6 +63,11 @@ BOOL applyInputStyleProps(ENRMInputFormatterStyle *style, const InputProps &newP
     changed = YES;
   }

+  if (newProps.lineHeight != oldProps.lineHeight) {
+    style.baseLineHeight = newProps.lineHeight > 0 ? newProps.lineHeight : 0;
+    changed = YES;
+  }
+
   if (newProps.markdownStyle.strong.color != oldProps.markdownStyle.strong.color) {
     if (isColorMeaningful(newProps.markdownStyle.strong.color)) {
       style.boldColor = RCTUIColorFromSharedColor(newProps.markdownStyle.strong.color);
@@ -103,3 +108,48 @@ BOOL applyInputStyleProps(ENRMInputFormatterStyle *style, const InputProps &newP

   return changed;
 }
+
+template <typename BlockStyleProps>
+BOOL applyInputBlockStyleProps(ENRMInputFormatterStyle *style, const BlockStyleProps &blockStyle)
+{
+  BOOL changed = NO;
+
+  CGFloat fontSize = blockStyle.fontSize > 0 ? blockStyle.fontSize : style.baseFont.pointSize;
+  UIFont *font = nil;
+  if (blockStyle.fontWeight.empty()) {
+    font = [UIFont systemFontOfSize:fontSize];
+  } else {
+    NSString *weightString = [NSString stringWithUTF8String:blockStyle.fontWeight.c_str()];
+    UIFontWeight weight = ENRMFontWeightFromString(weightString);
+    font = [UIFont systemFontOfSize:fontSize weight:weight];
+  }
+
+  if (!blockStyle.fontFamily.empty()) {
+    NSString *familyName = [NSString stringWithUTF8String:blockStyle.fontFamily.c_str()];
+    UIFont *customFont = [UIFont fontWithName:familyName size:fontSize];
+    if (customFont) {
+      font = customFont;
+    }
+  }
+
+  if (![style.baseFont isEqual:font]) {
+    style.baseFont = font;
+    changed = YES;
+  }
+
+  if (isColorMeaningful(blockStyle.color)) {
+    RCTUIColor *color = RCTUIColorFromSharedColor(blockStyle.color);
+    if (![style.baseTextColor isEqual:color]) {
+      style.baseTextColor = color;
+      changed = YES;
+    }
+  }
+
+  CGFloat lineHeight = blockStyle.lineHeight > 0 ? blockStyle.lineHeight : 0;
+  if (style.baseLineHeight != lineHeight) {
+    style.baseLineHeight = lineHeight;
+    changed = YES;
+  }
+
+  return changed;
+}
diff --git a/ios/internals/EnrichedMarkdownShadowNode.h b/ios/internals/EnrichedMarkdownShadowNode.h
index 656ae5b..48554b1 100644
--- a/ios/internals/EnrichedMarkdownShadowNode.h
+++ b/ios/internals/EnrichedMarkdownShadowNode.h
@@ -36,7 +36,7 @@ private:
   int localHeightRecalculationCounter_{0};

   // Creates mock view off-screen for initial measurement when real view doesn't exist
-  id setupMockEnrichedMarkdown_(CGFloat width) const;
+  id setupMockEnrichedMarkdown_(CGFloat width, NSString *markdown) const;
 };

 } // namespace facebook::react
diff --git a/ios/internals/EnrichedMarkdownShadowNode.mm b/ios/internals/EnrichedMarkdownShadowNode.mm
index 48f0dcd..858b273 100644
--- a/ios/internals/EnrichedMarkdownShadowNode.mm
+++ b/ios/internals/EnrichedMarkdownShadowNode.mm
@@ -32,7 +32,7 @@
 }

 /// Creates a mock view off-screen to measure content when real view isn't ready yet.
-id EnrichedMarkdownShadowNode::setupMockEnrichedMarkdown_(CGFloat width) const
+id EnrichedMarkdownShadowNode::setupMockEnrichedMarkdown_(CGFloat width, NSString *markdown) const
 {
   EnrichedMarkdown *mockView = [[EnrichedMarkdown alloc] initWithFrame:CGRectMake(20000, 20000, width, 1000)];

@@ -40,9 +40,7 @@
   [mockView updateProps:props oldProps:nullptr];

   // Render markdown synchronously for accurate measurement
-  const auto &typedProps = *std::static_pointer_cast<const EnrichedMarkdownProps>(props);
-  if (!typedProps.markdown.empty()) {
-    NSString *markdown = [NSString stringWithUTF8String:typedProps.markdown.c_str()];
+  if (markdown.length > 0) {
     [mockView renderMarkdownSynchronously:markdown];
   }

@@ -58,7 +56,9 @@

   CGFloat fontScale = typedProps.allowFontScaling ? RCTFontSizeMultiplier() : 1.0;

-  if (!typedProps.markdown.empty()) {
+  const BOOL usesNativeMarkdown = !typedProps.nativeMarkdownBlockId.empty();
+
+  if (!typedProps.markdown.empty() && !usesNativeMarkdown) {
     auto cacheKey = buildMeasurementCacheKey(typedProps, maxWidth, fontScale, MarkdownFlavor::GitHub);
     CachedSize cached;
     if (MeasurementCache::shared().get(cacheKey, cached)) {
@@ -76,15 +76,21 @@
       (RCTInternalGenericWeakWrapper *)unwrapManagedObject(getStateData().getComponentViewRef());
   EnrichedMarkdown *view = weakWrapper ? (EnrichedMarkdown *)weakWrapper.object : nil;

-  NSString *currentMarkdown = typedProps.markdown.empty() ? nil : @(typedProps.markdown.c_str());
-
   __block CGSize size;

   void (^measureBlock)(void) = ^{
+    NSString *currentMarkdown = nil;
+    if (usesNativeMarkdown) {
+      NSString *blockId = [[NSString alloc] initWithUTF8String:typedProps.nativeMarkdownBlockId.c_str()];
+      currentMarkdown = ENRMMarkdownForNativeBlockId(blockId);
+    } else if (!typedProps.markdown.empty()) {
+      currentMarkdown = @(typedProps.markdown.c_str());
+    }
+
     if (view && [view hasRenderedMarkdown:currentMarkdown]) {
       size = [view measureSize:maxWidth];
     } else {
-      EnrichedMarkdown *mockView = setupMockEnrichedMarkdown_(maxWidth);
+      EnrichedMarkdown *mockView = setupMockEnrichedMarkdown_(maxWidth, currentMarkdown);
       size = [mockView measureSize:maxWidth];
     }
   };
@@ -95,7 +101,7 @@
     dispatch_sync(dispatch_get_main_queue(), measureBlock);
   }

-  if (!typedProps.markdown.empty()) {
+  if (!typedProps.markdown.empty() && !usesNativeMarkdown) {
     auto cacheKey = buildMeasurementCacheKey(typedProps, maxWidth, fontScale, MarkdownFlavor::GitHub);
     MeasurementCache::shared().set(cacheKey, {size.width, size.height});
   }
diff --git a/ios/internals/EnrichedMarkdownTextShadowNode.h b/ios/internals/EnrichedMarkdownTextShadowNode.h
index b3523ed..b5dc607 100644
--- a/ios/internals/EnrichedMarkdownTextShadowNode.h
+++ b/ios/internals/EnrichedMarkdownTextShadowNode.h
@@ -38,7 +38,7 @@ private:
   int localHeightRecalculationCounter_{0};

   // Creates mock view off-screen for initial measurement when real view doesn't exist
-  id setupMockEnrichedMarkdownText_(CGFloat width) const;
+  id setupMockEnrichedMarkdownText_(CGFloat width, NSString *markdown) const;
 };

 } // namespace facebook::react
diff --git a/ios/internals/EnrichedMarkdownTextShadowNode.mm b/ios/internals/EnrichedMarkdownTextShadowNode.mm
index 417c39d..8c5b8ed 100644
--- a/ios/internals/EnrichedMarkdownTextShadowNode.mm
+++ b/ios/internals/EnrichedMarkdownTextShadowNode.mm
@@ -1,4 +1,5 @@
 #import "EnrichedMarkdownTextShadowNode.h"
+#import "EnrichedMarkdown.h"
 #import "EnrichedMarkdownText.h"
 #import <react/utils/ManagedObjectWrapper.h>
 #import <yoga/Yoga.h>
@@ -33,7 +34,7 @@
 }

 /// Creates a mock view off-screen to measure content when real view isn't ready yet.
-id EnrichedMarkdownTextShadowNode::setupMockEnrichedMarkdownText_(CGFloat width) const
+id EnrichedMarkdownTextShadowNode::setupMockEnrichedMarkdownText_(CGFloat width, NSString *markdown) const
 {
   EnrichedMarkdownText *mockView = [[EnrichedMarkdownText alloc] initWithFrame:CGRectMake(20000, 20000, width, 1000)];

@@ -41,9 +42,7 @@
   [mockView updateProps:props oldProps:nullptr];

   // Render markdown synchronously for accurate measurement
-  const auto &typedProps = *std::static_pointer_cast<const EnrichedMarkdownTextProps>(props);
-  if (!typedProps.markdown.empty()) {
-    NSString *markdown = [NSString stringWithUTF8String:typedProps.markdown.c_str()];
+  if (markdown.length > 0) {
     [mockView renderMarkdownSynchronously:markdown];
   }

@@ -61,7 +60,9 @@
   // This avoids the expensive mock view + synchronous md4c parse path for repeated content.
   CGFloat fontScale = typedProps.allowFontScaling ? RCTFontSizeMultiplier() : 1.0;

-  if (!typedProps.markdown.empty()) {
+  const BOOL usesNativeMarkdown = !typedProps.nativeMarkdownBlockId.empty();
+
+  if (!typedProps.markdown.empty() && !usesNativeMarkdown) {
     auto cacheKey = buildMeasurementCacheKey(typedProps, maxWidth, fontScale, MarkdownFlavor::CommonMark);
     CachedSize cached;
     if (MeasurementCache::shared().get(cacheKey, cached)) {
@@ -79,15 +80,21 @@
       (RCTInternalGenericWeakWrapper *)unwrapManagedObject(getStateData().getComponentViewRef());
   EnrichedMarkdownText *view = weakWrapper ? (EnrichedMarkdownText *)weakWrapper.object : nil;

-  NSString *currentMarkdown = typedProps.markdown.empty() ? nil : @(typedProps.markdown.c_str());
-
   __block CGSize size;

   void (^measureBlock)(void) = ^{
+    NSString *currentMarkdown = nil;
+    if (usesNativeMarkdown) {
+      NSString *blockId = [[NSString alloc] initWithUTF8String:typedProps.nativeMarkdownBlockId.c_str()];
+      currentMarkdown = ENRMMarkdownForNativeBlockId(blockId);
+    } else if (!typedProps.markdown.empty()) {
+      currentMarkdown = @(typedProps.markdown.c_str());
+    }
+
     if (view && [view hasRenderedMarkdown:currentMarkdown]) {
       size = [view measureSize:maxWidth];
     } else {
-      EnrichedMarkdownText *mockView = setupMockEnrichedMarkdownText_(maxWidth);
+      EnrichedMarkdownText *mockView = setupMockEnrichedMarkdownText_(maxWidth, currentMarkdown);
       size = [mockView measureSize:maxWidth];
     }
   };
@@ -98,7 +105,7 @@
     dispatch_sync(dispatch_get_main_queue(), measureBlock);
   }

-  if (!typedProps.markdown.empty()) {
+  if (!typedProps.markdown.empty() && !usesNativeMarkdown) {
     auto cacheKey = buildMeasurementCacheKey(typedProps, maxWidth, fontScale, MarkdownFlavor::CommonMark);
     MeasurementCache::shared().set(cacheKey, {size.width, size.height});
   }
diff --git a/ios/renderer/CodeRenderer.m b/ios/renderer/CodeRenderer.m
index e52a35c..358f27c 100644
--- a/ios/renderer/CodeRenderer.m
+++ b/ios/renderer/CodeRenderer.m
@@ -71,6 +71,10 @@ - (void)renderNode:(MarkdownASTNode *)node into:(NSMutableAttributedString *)out
     codeAttributes[@"BlockLineHeight"] = @(UIFontLineHeight(blockFont));

     [output setAttributes:codeAttributes range:range];
+
+    if (start > 0) {
+      [output addAttribute:NSKernAttributeName value:@(3.0) range:NSMakeRange(start - 1, 1)];
+    }
   }
 }

diff --git a/ios/utils/CodeBackground.m b/ios/utils/CodeBackground.m
index 5e5d5bf..8f2ac63 100644
--- a/ios/utils/CodeBackground.m
+++ b/ios/utils/CodeBackground.m
@@ -4,8 +4,9 @@

 NSString *const CodeAttributeName = @"Code";

-static const CGFloat kCodeBackgroundCornerRadius = 2.0;
+static const CGFloat kCodeBackgroundCornerRadius = 6.0;
 static const CGFloat kCodeBackgroundBorderWidth = 0.5;
+static const CGFloat kCodeBackgroundHorizontalPadding = 3.0;

 @implementation CodeBackground {
   StyleConfig *_config;
@@ -100,6 +101,14 @@ - (void)drawCodeBackgroundForRange:(NSRange)range
                                    finalRect.size.height = referenceHeight;
                                  }

+                                 if (isFirst) {
+                                   finalRect.origin.x -= kCodeBackgroundHorizontalPadding;
+                                   finalRect.size.width += kCodeBackgroundHorizontalPadding;
+                                 }
+                                 if (isLast) {
+                                   finalRect.size.width += kCodeBackgroundHorizontalPadding;
+                                 }
+
                                  [self drawBackgroundAndBorders:finalRect
                                                 backgroundColor:backgroundColor
                                                     borderColor:borderColor
@@ -120,14 +129,6 @@ - (void)drawBackgroundAndBorders:(CGRect)rect
   [backgroundColor setFill];
   [path fill];

-  if (borderColor) {
-    [borderColor setStroke];
-    UIBezierPath *strokePath =
-        (isFirst && isLast) ? path : [self openBorderPathForRect:rect isFirst:isFirst isLast:isLast];
-    strokePath.lineWidth = kCodeBackgroundBorderWidth;
-    BezierPathSetRoundStyle(strokePath);
-    [strokePath stroke];
-  }
 }

 - (UIBezierPath *)pathForRect:(CGRect)rect isFirst:(BOOL)isFirst isLast:(BOOL)isLast
diff --git a/ios/views/ENRMContextMenuTextView+macOS.h b/ios/views/ENRMContextMenuTextView+macOS.h
index 58d865a..1a5abb6 100644
--- a/ios/views/ENRMContextMenuTextView+macOS.h
+++ b/ios/views/ENRMContextMenuTextView+macOS.h
@@ -5,12 +5,14 @@
 #import "ENRMUIKit.h"

 typedef NSMenu *_Nullable (^ENRMContextMenuProvider)(NSMenu *baseMenu, NSTextView *textView);
+typedef void (^ENRMSelectionDragOutsideHandler)(NSString *direction, CGFloat windowX, CGFloat windowY);

 /// macOS-only ENRMPlatformTextView subclass that manages context menus
 /// and text deselection across sibling text views.
 @interface ENRMContextMenuTextView : ENRMPlatformTextView <NSMenuDelegate>

 @property (nonatomic, copy, nullable) ENRMContextMenuProvider contextMenuProvider;
+@property (nonatomic, copy, nullable) ENRMSelectionDragOutsideHandler selectionDragOutsideHandler;

 @end

diff --git a/ios/views/ENRMContextMenuTextView+macOS.m b/ios/views/ENRMContextMenuTextView+macOS.m
index 3da7fe6..d187bc7 100644
--- a/ios/views/ENRMContextMenuTextView+macOS.m
+++ b/ios/views/ENRMContextMenuTextView+macOS.m
@@ -3,6 +3,10 @@

 #if TARGET_OS_OSX

+@interface ENRMContextMenuTextView ()
+@property (nonatomic, strong) NSTimer *enrmSelectionDragTimer;
+@end
+
 @implementation ENRMContextMenuTextView

 - (NSScrollView *)enclosingScrollView
@@ -31,6 +35,7 @@ - (void)deselectAllInContainer
 - (void)mouseDown:(NSEvent *)event
 {
   [self deselectAllInContainer];
+  [self enrmStartSelectionDragPolling];
   [super mouseDown:event];

   if (self.selectedRange.length > 0) {
@@ -48,6 +53,61 @@ - (void)rightMouseDown:(NSEvent *)event
   [super rightMouseDown:event];
 }

+- (CGFloat)enrmTopOriginWindowYForWindowPoint:(NSPoint)windowPoint
+{
+  NSView *contentView = self.window.contentView;
+  if (contentView == nil) {
+    return windowPoint.y;
+  }
+  return NSHeight(contentView.bounds) - windowPoint.y;
+}
+
+- (void)enrmStartSelectionDragPolling
+{
+  [self enrmStopSelectionDragPolling];
+  __weak ENRMContextMenuTextView *weakSelf = self;
+  self.enrmSelectionDragTimer = [NSTimer timerWithTimeInterval:0.016
+                                                       repeats:YES
+                                                         block:^(__unused NSTimer *timer) {
+                                                           [weakSelf enrmPollSelectionDrag];
+                                                         }];
+  [[NSRunLoop currentRunLoop] addTimer:self.enrmSelectionDragTimer forMode:NSEventTrackingRunLoopMode];
+  [[NSRunLoop currentRunLoop] addTimer:self.enrmSelectionDragTimer forMode:NSRunLoopCommonModes];
+}
+
+- (void)enrmStopSelectionDragPolling
+{
+  [self.enrmSelectionDragTimer invalidate];
+  self.enrmSelectionDragTimer = nil;
+}
+
+- (void)enrmPollSelectionDrag
+{
+  if ((NSEvent.pressedMouseButtons & 1) == 0 || self.window == nil) {
+    [self enrmStopSelectionDragPolling];
+    return;
+  }
+
+  [self enrmHandleSelectionDragAtWindowPoint:self.window.mouseLocationOutsideOfEventStream];
+}
+
+- (void)enrmHandleSelectionDragAtWindowPoint:(NSPoint)windowPoint
+{
+  if (self.selectionDragOutsideHandler == nil || self.selectedRange.length == 0) {
+    return;
+  }
+
+  NSRect boundsInWindow = [self convertRect:self.bounds toView:nil];
+  CGFloat pointerY = windowPoint.y;
+  CGFloat windowX = windowPoint.x;
+  CGFloat windowY = [self enrmTopOriginWindowYForWindowPoint:windowPoint];
+  if (pointerY < NSMinY(boundsInWindow)) {
+    self.selectionDragOutsideHandler(@"down", windowX, windowY);
+  } else if (pointerY > NSMaxY(boundsInWindow)) {
+    self.selectionDragOutsideHandler(@"up", windowX, windowY);
+  }
+}
+
 - (NSMenu *)menuForEvent:(NSEvent *)event
 {
   NSMenu *baseMenu = [super menuForEvent:event];
diff --git a/ios/views/EnrichedMarkdownInternalText.h b/ios/views/EnrichedMarkdownInternalText.h
index 2bc6e14..e40dffa 100644
--- a/ios/views/EnrichedMarkdownInternalText.h
+++ b/ios/views/EnrichedMarkdownInternalText.h
@@ -35,6 +35,7 @@ NS_ASSUME_NONNULL_BEGIN

 #if TARGET_OS_OSX
 - (void)setContextMenuProvider:(ENRMContextMenuProvider)provider;
+- (void)setSelectionDragOutsideHandler:(ENRMSelectionDragOutsideHandler)handler;
 #endif

 @end
diff --git a/ios/views/EnrichedMarkdownInternalText.m b/ios/views/EnrichedMarkdownInternalText.m
index cdb8a0b..809cb30 100644
--- a/ios/views/EnrichedMarkdownInternalText.m
+++ b/ios/views/EnrichedMarkdownInternalText.m
@@ -155,6 +155,11 @@ - (void)setContextMenuProvider:(ENRMContextMenuProvider)provider
 {
   ((ENRMContextMenuTextView *)_textView).contextMenuProvider = provider;
 }
+
+- (void)setSelectionDragOutsideHandler:(ENRMSelectionDragOutsideHandler)handler
+{
+  ((ENRMContextMenuTextView *)_textView).selectionDragOutsideHandler = handler;
+}
 #endif

 @end
diff --git a/lib/module/EnrichedMarkdownNativeComponent.ts b/lib/module/EnrichedMarkdownNativeComponent.ts
index 06024b4..e2edb1b 100644
--- a/lib/module/EnrichedMarkdownNativeComponent.ts
+++ b/lib/module/EnrichedMarkdownNativeComponent.ts
@@ -206,6 +206,12 @@ export interface OnContextMenuItemPressEvent {
   selectionEnd: CodegenTypes.Int32;
 }

+export interface SelectionDragOutsideEvent {
+  direction: string;
+  windowX: CodegenTypes.Float;
+  windowY: CodegenTypes.Float;
+}
+
 /**
  * MD4C parser flags configuration.
  * Controls how the markdown parser interprets certain syntax.
@@ -230,7 +236,9 @@ export interface NativeProps extends ViewProps {
   /**
    * Markdown content to render.
    */
-  markdown: string;
+  markdown?: string;
+  nativeMarkdownBlockId?: string;
+  nativeMarkdownRevision?: CodegenTypes.Float;
   /**
    * Internal style configuration for markdown elements.
    * Always provided with complete defaults via normalizeMarkdownStyle.
@@ -322,6 +330,7 @@ export interface NativeProps extends ViewProps {
    * Receives the item label, the currently selected text, and the selection range.
    */
   onContextMenuItemPress?: CodegenTypes.BubblingEventHandler<OnContextMenuItemPressEvent>;
+  onSelectionDragOutside?: CodegenTypes.BubblingEventHandler<SelectionDragOutsideEvent>;
 }

 export default codegenNativeComponent<NativeProps>('EnrichedMarkdown', {
diff --git a/lib/module/EnrichedMarkdownTextInput.js b/lib/module/EnrichedMarkdownTextInput.js
index 4b01cfc..7e14896 100644
--- a/lib/module/EnrichedMarkdownTextInput.js
+++ b/lib/module/EnrichedMarkdownTextInput.js
@@ -28,6 +28,8 @@ export const EnrichedMarkdownTextInput = ({
   onChangeText,
   onChangeMarkdown,
   onChangeSelection,
+  onSelectionDragOutside,
+  onVerticalNavigationOutside,
   onChangeState,
   onCaretRectChange,
   onLinkDetected,
@@ -101,6 +103,21 @@ export const EnrichedMarkdownTextInput = ({
       end
     });
   }, [onChangeSelection]);
+  const handleSelectionDragOutside = useCallback(e => {
+    const event = "nativeEvent" in e ? e.nativeEvent : e;
+    onSelectionDragOutside?.({
+      direction: event.direction,
+      windowX: event.windowX,
+      windowY: event.windowY
+    });
+  }, [onSelectionDragOutside]);
+  const handleVerticalNavigationOutside = useCallback(e => {
+    const event = "nativeEvent" in e ? e.nativeEvent : e;
+    onVerticalNavigationOutside?.({
+      direction: event.direction,
+      preferredX: event.preferredX
+    });
+  }, [onVerticalNavigationOutside]);
   const handleChangeState = useCallback(e => {
     const {
       bold,
@@ -198,6 +215,7 @@ export const EnrichedMarkdownTextInput = ({
       blur: () => Commands.blur(commandRef),
       setValue: markdown => Commands.setValue(commandRef, markdown),
       setSelection: (start, end) => Commands.setSelection(commandRef, start, end),
+      setSelectionForVerticalNavigation: (direction, preferredX) => Commands.setSelectionForVerticalNavigation(commandRef, direction, preferredX),
       toggleBold: () => Commands.toggleBold(commandRef),
       toggleItalic: () => Commands.toggleItalic(commandRef),
       toggleUnderline: () => Commands.toggleUnderline(commandRef),
@@ -242,6 +260,8 @@ export const EnrichedMarkdownTextInput = ({
     onChangeText: handleChangeText,
     onChangeMarkdown: handleChangeMarkdown,
     onChangeSelection: handleChangeSelection,
+    onSelectionDragOutside: handleSelectionDragOutside,
+    onVerticalNavigationOutside: handleVerticalNavigationOutside,
     onChangeState: handleChangeState,
     onLinkDetected: handleLinkDetected,
     onInputFocus: handleFocus,
diff --git a/lib/module/EnrichedMarkdownTextInputNativeComponent.ts b/lib/module/EnrichedMarkdownTextInputNativeComponent.ts
index d936825..8a18007 100644
--- a/lib/module/EnrichedMarkdownTextInputNativeComponent.ts
+++ b/lib/module/EnrichedMarkdownTextInputNativeComponent.ts
@@ -9,6 +9,14 @@ import {
 import type React from 'react';

 interface MarkdownTextInputStyleInternal {
+  paragraph: InputBlockStyleInternal;
+  h1: InputBlockStyleInternal;
+  h2: InputBlockStyleInternal;
+  h3: InputBlockStyleInternal;
+  h4: InputBlockStyleInternal;
+  h5: InputBlockStyleInternal;
+  h6: InputBlockStyleInternal;
+  codeBlock: InputBlockStyleInternal;
   strong: {
     color?: ColorValue;
   };
@@ -25,6 +33,14 @@ interface MarkdownTextInputStyleInternal {
   };
 }

+interface InputBlockStyleInternal {
+  fontSize: CodegenTypes.Float;
+  fontFamily: string;
+  fontWeight: string;
+  color: ColorValue;
+  lineHeight: CodegenTypes.Float;
+}
+
 interface TargetedEvent {
   target: CodegenTypes.Int32;
 }
@@ -42,6 +58,17 @@ export interface OnChangeSelectionEvent {
   end: CodegenTypes.Int32;
 }

+export interface OnSelectionDragOutsideEvent {
+  direction: string;
+  windowX: CodegenTypes.Float;
+  windowY: CodegenTypes.Float;
+}
+
+export interface OnVerticalNavigationOutsideEvent {
+  direction: string;
+  preferredX: CodegenTypes.Double;
+}
+
 export interface OnChangeStateEvent {
   bold: { isActive: boolean };
   italic: { isActive: boolean };
@@ -186,6 +213,8 @@ export interface NativeProps extends ViewProps {
   onChangeText?: CodegenTypes.DirectEventHandler<OnChangeTextEvent>;
   onChangeMarkdown?: CodegenTypes.DirectEventHandler<OnChangeMarkdownEvent>;
   onChangeSelection?: CodegenTypes.DirectEventHandler<OnChangeSelectionEvent>;
+  onSelectionDragOutside?: CodegenTypes.DirectEventHandler<OnSelectionDragOutsideEvent>;
+  onVerticalNavigationOutside?: CodegenTypes.DirectEventHandler<OnVerticalNavigationOutsideEvent>;
   onChangeState?: CodegenTypes.DirectEventHandler<OnChangeStateEvent>;
   onInputFocus?: CodegenTypes.DirectEventHandler<TargetedEvent>;
   onInputBlur?: CodegenTypes.DirectEventHandler<TargetedEvent>;
@@ -210,6 +239,11 @@ interface NativeCommands {
     start: CodegenTypes.Int32,
     end: CodegenTypes.Int32
   ) => void;
+  setSelectionForVerticalNavigation: (
+    viewRef: React.ElementRef<ComponentType>,
+    direction: string,
+    preferredX: CodegenTypes.Double
+  ) => void;
   toggleBold: (viewRef: React.ElementRef<ComponentType>) => void;
   toggleItalic: (viewRef: React.ElementRef<ComponentType>) => void;
   toggleUnderline: (viewRef: React.ElementRef<ComponentType>) => void;
@@ -238,6 +272,7 @@ export const Commands: NativeCommands = codegenNativeCommands<NativeCommands>({
     'blur',
     'setValue',
     'setSelection',
+    'setSelectionForVerticalNavigation',
     'toggleBold',
     'toggleItalic',
     'toggleUnderline',
diff --git a/lib/module/EnrichedMarkdownTextNativeComponent.ts b/lib/module/EnrichedMarkdownTextNativeComponent.ts
index e238ecf..0f7b33d 100644
--- a/lib/module/EnrichedMarkdownTextNativeComponent.ts
+++ b/lib/module/EnrichedMarkdownTextNativeComponent.ts
@@ -206,6 +206,12 @@ export interface OnContextMenuItemPressEvent {
   selectionEnd: CodegenTypes.Int32;
 }

+export interface SelectionDragOutsideEvent {
+  direction: string;
+  windowX: CodegenTypes.Float;
+  windowY: CodegenTypes.Float;
+}
+
 /**
  * MD4C parser flags configuration.
  * Controls how the markdown parser interprets certain syntax.
@@ -230,7 +236,9 @@ export interface NativeProps extends ViewProps {
   /**
    * Markdown content to render.
    */
-  markdown: string;
+  markdown?: string;
+  nativeMarkdownBlockId?: string;
+  nativeMarkdownRevision?: CodegenTypes.Float;
   /**
    * Internal style configuration for markdown elements.
    * Always provided with complete defaults via normalizeMarkdownStyle.
@@ -321,6 +329,7 @@ export interface NativeProps extends ViewProps {
    * Receives the item label, the currently selected text, and the selection range.
    */
   onContextMenuItemPress?: CodegenTypes.BubblingEventHandler<OnContextMenuItemPressEvent>;
+  onSelectionDragOutside?: CodegenTypes.BubblingEventHandler<SelectionDragOutsideEvent>;
 }

 export default codegenNativeComponent<NativeProps>('EnrichedMarkdownText', {
diff --git a/lib/module/native/EnrichedMarkdownText.js b/lib/module/native/EnrichedMarkdownText.js
index 490b567..bd0410c 100644
--- a/lib/module/native/EnrichedMarkdownText.js
+++ b/lib/module/native/EnrichedMarkdownText.js
@@ -26,6 +26,9 @@ export const EnrichedMarkdownText = ({
   streamingAnimation = false,
   spoilerOverlay = 'particles',
   contextMenuItems,
+  nativeMarkdownBlockId,
+  nativeMarkdownRevision,
+  onSelectionDragOutside,
   ...rest
 }) => {
   const normalizedStyleRef = useRef(null);
@@ -98,8 +101,19 @@ export const EnrichedMarkdownText = ({
       text
     });
   }, [onTaskListItemPress]);
+  const handleSelectionDragOutside = useCallback(e => {
+    onSelectionDragOutside?.(e.nativeEvent);
+  }, [onSelectionDragOutside]);
+  const markdownProps = markdown === undefined ? {} : {
+    markdown
+  };
+  const nativeMarkdownProps = nativeMarkdownBlockId === undefined ? {} : {
+    nativeMarkdownBlockId,
+    nativeMarkdownRevision
+  };
   const sharedProps = {
-    markdown,
+    ...markdownProps,
+    ...nativeMarkdownProps,
     markdownStyle: normalizedStyle,
     onLinkPress: handleLinkPress,
     onLinkLongPress: handleLinkLongPress,
@@ -115,6 +129,7 @@ export const EnrichedMarkdownText = ({
     style: containerStyle,
     contextMenuItems: nativeContextMenuItems,
     onContextMenuItemPress: handleContextMenuItemPress,
+    onSelectionDragOutside: handleSelectionDragOutside,
     ...rest
   };
   if (flavor === 'github') {
diff --git a/lib/module/normalizeMarkdownTextInputStyle.js b/lib/module/normalizeMarkdownTextInputStyle.js
index 0bafc38..32d998f 100644
--- a/lib/module/normalizeMarkdownTextInputStyle.js
+++ b/lib/module/normalizeMarkdownTextInputStyle.js
@@ -1,11 +1,28 @@
 "use strict";

 import { processColor } from 'react-native';
+import { normalizeMarkdownStyle } from "./normalizeMarkdownStyle.js";
 import { normalizeColor } from "./styleUtils.js";
 const DEFAULT_LINK_COLOR = '#2563EB';
 const DEFAULT_SPOILER_COLOR = '#374151';
 const DEFAULT_SPOILER_BG_COLOR = '#E5E7EB';
+const pickInputBlockStyle = style => ({
+  fontSize: style.fontSize,
+  fontFamily: style.fontFamily,
+  fontWeight: style.fontWeight,
+  color: style.color,
+  lineHeight: style.lineHeight
+});
+const defaultMarkdownStyle = normalizeMarkdownStyle({});
 const defaultInternal = Object.freeze({
+  paragraph: pickInputBlockStyle(defaultMarkdownStyle.paragraph),
+  h1: pickInputBlockStyle(defaultMarkdownStyle.h1),
+  h2: pickInputBlockStyle(defaultMarkdownStyle.h2),
+  h3: pickInputBlockStyle(defaultMarkdownStyle.h3),
+  h4: pickInputBlockStyle(defaultMarkdownStyle.h4),
+  h5: pickInputBlockStyle(defaultMarkdownStyle.h5),
+  h6: pickInputBlockStyle(defaultMarkdownStyle.h6),
+  codeBlock: pickInputBlockStyle(defaultMarkdownStyle.codeBlock),
   strong: {
     color: undefined
   },
@@ -30,7 +47,16 @@ export const normalizeMarkdownTextInputStyle = style => {
   if (style === cachedInput && cachedResult) {
     return cachedResult;
   }
+  const normalizedBlockStyle = normalizeMarkdownStyle(style);
   const result = {
+    paragraph: pickInputBlockStyle(normalizedBlockStyle.paragraph),
+    h1: pickInputBlockStyle(normalizedBlockStyle.h1),
+    h2: pickInputBlockStyle(normalizedBlockStyle.h2),
+    h3: pickInputBlockStyle(normalizedBlockStyle.h3),
+    h4: pickInputBlockStyle(normalizedBlockStyle.h4),
+    h5: pickInputBlockStyle(normalizedBlockStyle.h5),
+    h6: pickInputBlockStyle(normalizedBlockStyle.h6),
+    codeBlock: pickInputBlockStyle(normalizedBlockStyle.codeBlock),
     strong: {
       color: normalizeColor(style.strong?.color)
     },
diff --git a/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts b/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts
index 567cc1e..38032ef 100644
--- a/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts
+++ b/lib/typescript/src/EnrichedMarkdownNativeComponent.d.ts
@@ -194,7 +194,9 @@ export interface NativeProps extends ViewProps {
     /**
      * Markdown content to render.
      */
-    markdown: string;
+    markdown?: string;
+    nativeMarkdownBlockId?: string;
+    nativeMarkdownRevision?: CodegenTypes.Float;
     /**
      * Internal style configuration for markdown elements.
      * Always provided with complete defaults via normalizeMarkdownStyle.
diff --git a/lib/typescript/src/EnrichedMarkdownTextInput.d.ts b/lib/typescript/src/EnrichedMarkdownTextInput.d.ts
index fb36024..da1485c 100644
--- a/lib/typescript/src/EnrichedMarkdownTextInput.d.ts
+++ b/lib/typescript/src/EnrichedMarkdownTextInput.d.ts
@@ -2,7 +2,8 @@ import { type OnLinkDetected } from './EnrichedMarkdownTextInputNativeComponent'
 export type { OnLinkDetected } from './EnrichedMarkdownTextInputNativeComponent';
 import type { HostInstance, ViewStyle, TextStyle, ColorValue } from 'react-native';
 import type { RefObject } from 'react';
-export interface MarkdownTextInputStyle {
+import type { MarkdownStyle } from './types/MarkdownStyle';
+export type MarkdownTextInputStyle = MarkdownStyle & {
     strong?: {
         color?: string;
     };
@@ -17,7 +18,7 @@ export interface MarkdownTextInputStyle {
         color?: string;
         backgroundColor?: string;
     };
-}
+};
 export interface StyleState {
     bold: {
         isActive: boolean;
@@ -65,6 +66,7 @@ export interface EnrichedMarkdownTextInputInstance {
     measureLayout: HostInstance['measureLayout'];
     setValue: (markdown: string) => void;
     setSelection: (start: number, end: number) => void;
+    setSelectionForVerticalNavigation: (direction: 'up' | 'down' | string, preferredX: number) => void;
     toggleBold: () => void;
     toggleItalic: () => void;
     toggleUnderline: () => void;
@@ -96,6 +98,15 @@ export interface EnrichedMarkdownTextInputProps {
         start: number;
         end: number;
     }) => void;
+    onSelectionDragOutside?: (event: {
+        direction: string;
+        windowX: number;
+        windowY: number;
+    }) => void;
+    onVerticalNavigationOutside?: (event: {
+        direction: string;
+        preferredX: number;
+    }) => void;
     onChangeState?: (state: StyleState) => void;
     onCaretRectChange?: (rect: CaretRect) => void;
     onLinkDetected?: (event: OnLinkDetected) => void;
@@ -104,6 +115,6 @@ export interface EnrichedMarkdownTextInputProps {
     contextMenuItems?: ContextMenuItem[];
     linkRegex?: RegExp | null;
 }
-export declare const EnrichedMarkdownTextInput: ({ ref, markdownStyle, style, defaultValue, placeholder, placeholderTextColor, editable, autoFocus, scrollEnabled, autoCapitalize, multiline, cursorColor, selectionColor, onChangeText, onChangeMarkdown, onChangeSelection, onChangeState, onCaretRectChange, onLinkDetected, onFocus, onBlur, contextMenuItems, linkRegex: _linkRegex, }: EnrichedMarkdownTextInputProps) => import("react/jsx-runtime").JSX.Element;
+export declare const EnrichedMarkdownTextInput: ({ ref, markdownStyle, style, defaultValue, placeholder, placeholderTextColor, editable, autoFocus, scrollEnabled, autoCapitalize, multiline, cursorColor, selectionColor, onChangeText, onChangeMarkdown, onChangeSelection, onSelectionDragOutside, onVerticalNavigationOutside, onChangeState, onCaretRectChange, onLinkDetected, onFocus, onBlur, contextMenuItems, linkRegex: _linkRegex, }: EnrichedMarkdownTextInputProps) => import("react/jsx-runtime").JSX.Element;
 export default EnrichedMarkdownTextInput;
 //# sourceMappingURL=EnrichedMarkdownTextInput.d.ts.map
\ No newline at end of file
diff --git a/lib/typescript/src/EnrichedMarkdownTextInputNativeComponent.d.ts b/lib/typescript/src/EnrichedMarkdownTextInputNativeComponent.d.ts
index fb5ebe3..f7d3b7c 100644
--- a/lib/typescript/src/EnrichedMarkdownTextInputNativeComponent.d.ts
+++ b/lib/typescript/src/EnrichedMarkdownTextInputNativeComponent.d.ts
@@ -1,6 +1,14 @@
 import { type ViewProps, type ColorValue, type HostComponent, type CodegenTypes } from 'react-native';
 import type React from 'react';
 interface MarkdownTextInputStyleInternal {
+    paragraph: InputBlockStyleInternal;
+    h1: InputBlockStyleInternal;
+    h2: InputBlockStyleInternal;
+    h3: InputBlockStyleInternal;
+    h4: InputBlockStyleInternal;
+    h5: InputBlockStyleInternal;
+    h6: InputBlockStyleInternal;
+    codeBlock: InputBlockStyleInternal;
     strong: {
         color?: ColorValue;
     };
@@ -16,6 +24,13 @@ interface MarkdownTextInputStyleInternal {
         backgroundColor: ColorValue;
     };
 }
+interface InputBlockStyleInternal {
+    fontSize: CodegenTypes.Float;
+    fontFamily: string;
+    fontWeight: string;
+    color: ColorValue;
+    lineHeight: CodegenTypes.Float;
+}
 interface TargetedEvent {
     target: CodegenTypes.Int32;
 }
@@ -29,6 +44,15 @@ export interface OnChangeSelectionEvent {
     start: CodegenTypes.Int32;
     end: CodegenTypes.Int32;
 }
+export interface OnSelectionDragOutsideEvent {
+    direction: string;
+    windowX: CodegenTypes.Float;
+    windowY: CodegenTypes.Float;
+}
+export interface OnVerticalNavigationOutsideEvent {
+    direction: string;
+    preferredX: CodegenTypes.Double;
+}
 export interface OnChangeStateEvent {
     bold: {
         isActive: boolean;
@@ -182,6 +206,8 @@ export interface NativeProps extends ViewProps {
     onChangeText?: CodegenTypes.DirectEventHandler<OnChangeTextEvent>;
     onChangeMarkdown?: CodegenTypes.DirectEventHandler<OnChangeMarkdownEvent>;
     onChangeSelection?: CodegenTypes.DirectEventHandler<OnChangeSelectionEvent>;
+    onSelectionDragOutside?: CodegenTypes.DirectEventHandler<OnSelectionDragOutsideEvent>;
+    onVerticalNavigationOutside?: CodegenTypes.DirectEventHandler<OnVerticalNavigationOutsideEvent>;
     onChangeState?: CodegenTypes.DirectEventHandler<OnChangeStateEvent>;
     onInputFocus?: CodegenTypes.DirectEventHandler<TargetedEvent>;
     onInputBlur?: CodegenTypes.DirectEventHandler<TargetedEvent>;
@@ -197,6 +223,7 @@ interface NativeCommands {
     blur: (viewRef: React.ElementRef<ComponentType>) => void;
     setValue: (viewRef: React.ElementRef<ComponentType>, markdown: string) => void;
     setSelection: (viewRef: React.ElementRef<ComponentType>, start: CodegenTypes.Int32, end: CodegenTypes.Int32) => void;
+    setSelectionForVerticalNavigation: (viewRef: React.ElementRef<ComponentType>, direction: string, preferredX: CodegenTypes.Double) => void;
     toggleBold: (viewRef: React.ElementRef<ComponentType>) => void;
     toggleItalic: (viewRef: React.ElementRef<ComponentType>) => void;
     toggleUnderline: (viewRef: React.ElementRef<ComponentType>) => void;
diff --git a/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts b/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts
index 4254d5d..0919511 100644
--- a/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts
+++ b/lib/typescript/src/EnrichedMarkdownTextNativeComponent.d.ts
@@ -194,7 +194,9 @@ export interface NativeProps extends ViewProps {
     /**
      * Markdown content to render.
      */
-    markdown: string;
+    markdown?: string;
+    nativeMarkdownBlockId?: string;
+    nativeMarkdownRevision?: CodegenTypes.Float;
     /**
      * Internal style configuration for markdown elements.
      * Always provided with complete defaults via normalizeMarkdownStyle.
diff --git a/lib/typescript/src/normalizeMarkdownTextInputStyle.d.ts b/lib/typescript/src/normalizeMarkdownTextInputStyle.d.ts
index ddc0716..3c39bc2 100644
--- a/lib/typescript/src/normalizeMarkdownTextInputStyle.d.ts
+++ b/lib/typescript/src/normalizeMarkdownTextInputStyle.d.ts
@@ -1,6 +1,21 @@
 import { type ColorValue } from 'react-native';
 import type { MarkdownTextInputStyle } from './EnrichedMarkdownTextInput';
+interface InputBlockStyleInternal {
+    fontSize: number;
+    fontFamily: string;
+    fontWeight: string;
+    color: ColorValue;
+    lineHeight: number;
+}
 interface MarkdownTextInputStyleInternal {
+    paragraph: InputBlockStyleInternal;
+    h1: InputBlockStyleInternal;
+    h2: InputBlockStyleInternal;
+    h3: InputBlockStyleInternal;
+    h4: InputBlockStyleInternal;
+    h5: InputBlockStyleInternal;
+    h6: InputBlockStyleInternal;
+    codeBlock: InputBlockStyleInternal;
     strong: {
         color?: ColorValue;
     };
diff --git a/lib/typescript/src/types/MarkdownTextProps.d.ts b/lib/typescript/src/types/MarkdownTextProps.d.ts
index b912aa6..f38293b 100644
--- a/lib/typescript/src/types/MarkdownTextProps.d.ts
+++ b/lib/typescript/src/types/MarkdownTextProps.d.ts
@@ -1,6 +1,6 @@
 import type { ViewProps, ViewStyle, TextStyle } from 'react-native';
 import type { MarkdownStyle, Md4cFlags } from './MarkdownStyle';
-import type { LinkPressEvent, LinkLongPressEvent, TaskListItemPressEvent } from './events';
+import type { LinkPressEvent, LinkLongPressEvent, SelectionDragOutsideEvent, TaskListItemPressEvent } from './events';
 /**
  * Public context menu item. Each item includes a JS-side `onPress` callback
  * that is called when the user taps the item in the selection context menu.
@@ -22,7 +22,17 @@ export interface EnrichedMarkdownTextProps extends Omit<ViewProps, 'style'> {
      * Markdown content to render.
      * @platform ios, android, web
      */
-    markdown: string;
+    markdown?: string;
+    /**
+     * Native markdown block id to render without passing markdown through JS.
+     * @platform ios
+     */
+    nativeMarkdownBlockId?: string;
+    /**
+     * Native markdown block revision used to invalidate render and measurement.
+     * @platform ios
+     */
+    nativeMarkdownRevision?: number;
     /**
      * Style configuration for markdown elements.
      * @platform ios, android, web
@@ -66,6 +76,7 @@ export interface EnrichedMarkdownTextProps extends Omit<ViewProps, 'style'> {
      * @platform ios, android, web
      */
     onTaskListItemPress?: (event: TaskListItemPressEvent) => void;
+    onSelectionDragOutside?: (event: SelectionDragOutsideEvent) => void;
     /**
      * Controls whether the system link preview is shown on long press (iOS only).
      *
diff --git a/lib/typescript/src/types/events.d.ts b/lib/typescript/src/types/events.d.ts
index 0b62023..c04e64b 100644
--- a/lib/typescript/src/types/events.d.ts
+++ b/lib/typescript/src/types/events.d.ts
@@ -26,4 +26,9 @@ export interface OnContextMenuItemPressEvent {
     selectionStart: number;
     selectionEnd: number;
 }
+export interface SelectionDragOutsideEvent {
+    direction: string;
+    windowX: number;
+    windowY: number;
+}
 //# sourceMappingURL=events.d.ts.map
\ No newline at end of file
diff --git a/src/EnrichedMarkdownNativeComponent.ts b/src/EnrichedMarkdownNativeComponent.ts
index 06024b4..e2edb1b 100644
--- a/src/EnrichedMarkdownNativeComponent.ts
+++ b/src/EnrichedMarkdownNativeComponent.ts
@@ -206,6 +206,12 @@ export interface OnContextMenuItemPressEvent {
   selectionEnd: CodegenTypes.Int32;
 }

+export interface SelectionDragOutsideEvent {
+  direction: string;
+  windowX: CodegenTypes.Float;
+  windowY: CodegenTypes.Float;
+}
+
 /**
  * MD4C parser flags configuration.
  * Controls how the markdown parser interprets certain syntax.
@@ -230,7 +236,9 @@ export interface NativeProps extends ViewProps {
   /**
    * Markdown content to render.
    */
-  markdown: string;
+  markdown?: string;
+  nativeMarkdownBlockId?: string;
+  nativeMarkdownRevision?: CodegenTypes.Float;
   /**
    * Internal style configuration for markdown elements.
    * Always provided with complete defaults via normalizeMarkdownStyle.
@@ -322,6 +330,7 @@ export interface NativeProps extends ViewProps {
    * Receives the item label, the currently selected text, and the selection range.
    */
   onContextMenuItemPress?: CodegenTypes.BubblingEventHandler<OnContextMenuItemPressEvent>;
+  onSelectionDragOutside?: CodegenTypes.BubblingEventHandler<SelectionDragOutsideEvent>;
 }

 export default codegenNativeComponent<NativeProps>('EnrichedMarkdown', {
diff --git a/src/EnrichedMarkdownTextInput.tsx b/src/EnrichedMarkdownTextInput.tsx
index 6ea44db..6b7cac1 100644
--- a/src/EnrichedMarkdownTextInput.tsx
+++ b/src/EnrichedMarkdownTextInput.tsx
@@ -12,6 +12,8 @@ import EnrichedMarkdownTextInputNativeComponent, {
   type OnChangeTextEvent,
   type OnChangeMarkdownEvent,
   type OnChangeSelectionEvent,
+  type OnSelectionDragOutsideEvent,
+  type OnVerticalNavigationOutsideEvent,
   type OnChangeStateEvent,
   type OnRequestMarkdownResultEvent,
   type OnRequestCaretRectResultEvent,
@@ -30,10 +32,11 @@ import type {
 import { normalizeMarkdownTextInputStyle } from './normalizeMarkdownTextInputStyle';
 import { toNativeRegexConfig } from './utils/regexParser';
 import type { RefObject } from 'react';
+import type { MarkdownStyle } from './types/MarkdownStyle';

 type NativeRef = HostInstance;

-export interface MarkdownTextInputStyle {
+export type MarkdownTextInputStyle = MarkdownStyle & {
   strong?: {
     color?: string;
   };
@@ -48,7 +51,7 @@ export interface MarkdownTextInputStyle {
     color?: string;
     backgroundColor?: string;
   };
-}
+};

 export interface StyleState {
   bold: { isActive: boolean };
@@ -85,6 +88,10 @@ export interface EnrichedMarkdownTextInputInstance {
   measureLayout: HostInstance['measureLayout'];
   setValue: (markdown: string) => void;
   setSelection: (start: number, end: number) => void;
+  setSelectionForVerticalNavigation: (
+    direction: 'up' | 'down' | string,
+    preferredX: number
+  ) => void;
   toggleBold: () => void;
   toggleItalic: () => void;
   toggleUnderline: () => void;
@@ -114,6 +121,15 @@ export interface EnrichedMarkdownTextInputProps {
   onChangeText?: (text: string) => void;
   onChangeMarkdown?: (markdown: string) => void;
   onChangeSelection?: (selection: { start: number; end: number }) => void;
+  onSelectionDragOutside?: (event: {
+    direction: string;
+    windowX: number;
+    windowY: number;
+  }) => void;
+  onVerticalNavigationOutside?: (event: {
+    direction: string;
+    preferredX: number;
+  }) => void;
   onChangeState?: (state: StyleState) => void;
   onCaretRectChange?: (rect: CaretRect) => void;
   onLinkDetected?: (event: OnLinkDetected) => void;
@@ -154,6 +170,8 @@ export const EnrichedMarkdownTextInput = ({
   onChangeText,
   onChangeMarkdown,
   onChangeSelection,
+  onSelectionDragOutside,
+  onVerticalNavigationOutside,
   onChangeState,
   onCaretRectChange,
   onLinkDetected,
@@ -241,6 +259,29 @@ export const EnrichedMarkdownTextInput = ({
     [onChangeSelection]
   );

+  const handleSelectionDragOutside = useCallback(
+    (e: NativeSyntheticEvent<OnSelectionDragOutsideEvent> | OnSelectionDragOutsideEvent) => {
+      const event = "nativeEvent" in e ? e.nativeEvent : e;
+      onSelectionDragOutside?.({
+        direction: event.direction,
+        windowX: event.windowX,
+        windowY: event.windowY,
+      });
+    },
+    [onSelectionDragOutside]
+  );
+
+  const handleVerticalNavigationOutside = useCallback(
+    (e: NativeSyntheticEvent<OnVerticalNavigationOutsideEvent> | OnVerticalNavigationOutsideEvent) => {
+      const event = "nativeEvent" in e ? e.nativeEvent : e;
+      onVerticalNavigationOutside?.({
+        direction: event.direction,
+        preferredX: event.preferredX,
+      });
+    },
+    [onVerticalNavigationOutside]
+  );
+
   const handleChangeState = useCallback(
     (e: NativeSyntheticEvent<OnChangeStateEvent>) => {
       const { bold, italic, underline, strikethrough, spoiler, link } =
@@ -331,6 +372,8 @@ export const EnrichedMarkdownTextInput = ({
       setValue: (markdown) => Commands.setValue(commandRef, markdown),
       setSelection: (start, end) =>
         Commands.setSelection(commandRef, start, end),
+      setSelectionForVerticalNavigation: (direction, preferredX) =>
+        Commands.setSelectionForVerticalNavigation(commandRef, direction, preferredX),
       toggleBold: () => Commands.toggleBold(commandRef),
       toggleItalic: () => Commands.toggleItalic(commandRef),
       toggleUnderline: () => Commands.toggleUnderline(commandRef),
@@ -378,6 +421,12 @@ export const EnrichedMarkdownTextInput = ({
       onChangeSelection={
         handleChangeSelection as NativeProps['onChangeSelection']
       }
+      onSelectionDragOutside={
+        handleSelectionDragOutside as NativeProps['onSelectionDragOutside']
+      }
+      onVerticalNavigationOutside={
+        handleVerticalNavigationOutside as NativeProps['onVerticalNavigationOutside']
+      }
       onChangeState={handleChangeState as NativeProps['onChangeState']}
       onLinkDetected={handleLinkDetected as NativeProps['onLinkDetected']}
       onInputFocus={handleFocus as NativeProps['onInputFocus']}
diff --git a/src/EnrichedMarkdownTextInputNativeComponent.ts b/src/EnrichedMarkdownTextInputNativeComponent.ts
index d936825..8a18007 100644
--- a/src/EnrichedMarkdownTextInputNativeComponent.ts
+++ b/src/EnrichedMarkdownTextInputNativeComponent.ts
@@ -9,6 +9,14 @@ import {
 import type React from 'react';

 interface MarkdownTextInputStyleInternal {
+  paragraph: InputBlockStyleInternal;
+  h1: InputBlockStyleInternal;
+  h2: InputBlockStyleInternal;
+  h3: InputBlockStyleInternal;
+  h4: InputBlockStyleInternal;
+  h5: InputBlockStyleInternal;
+  h6: InputBlockStyleInternal;
+  codeBlock: InputBlockStyleInternal;
   strong: {
     color?: ColorValue;
   };
@@ -25,6 +33,14 @@ interface MarkdownTextInputStyleInternal {
   };
 }

+interface InputBlockStyleInternal {
+  fontSize: CodegenTypes.Float;
+  fontFamily: string;
+  fontWeight: string;
+  color: ColorValue;
+  lineHeight: CodegenTypes.Float;
+}
+
 interface TargetedEvent {
   target: CodegenTypes.Int32;
 }
@@ -42,6 +58,17 @@ export interface OnChangeSelectionEvent {
   end: CodegenTypes.Int32;
 }

+export interface OnSelectionDragOutsideEvent {
+  direction: string;
+  windowX: CodegenTypes.Float;
+  windowY: CodegenTypes.Float;
+}
+
+export interface OnVerticalNavigationOutsideEvent {
+  direction: string;
+  preferredX: CodegenTypes.Double;
+}
+
 export interface OnChangeStateEvent {
   bold: { isActive: boolean };
   italic: { isActive: boolean };
@@ -186,6 +213,8 @@ export interface NativeProps extends ViewProps {
   onChangeText?: CodegenTypes.DirectEventHandler<OnChangeTextEvent>;
   onChangeMarkdown?: CodegenTypes.DirectEventHandler<OnChangeMarkdownEvent>;
   onChangeSelection?: CodegenTypes.DirectEventHandler<OnChangeSelectionEvent>;
+  onSelectionDragOutside?: CodegenTypes.DirectEventHandler<OnSelectionDragOutsideEvent>;
+  onVerticalNavigationOutside?: CodegenTypes.DirectEventHandler<OnVerticalNavigationOutsideEvent>;
   onChangeState?: CodegenTypes.DirectEventHandler<OnChangeStateEvent>;
   onInputFocus?: CodegenTypes.DirectEventHandler<TargetedEvent>;
   onInputBlur?: CodegenTypes.DirectEventHandler<TargetedEvent>;
@@ -210,6 +239,11 @@ interface NativeCommands {
     start: CodegenTypes.Int32,
     end: CodegenTypes.Int32
   ) => void;
+  setSelectionForVerticalNavigation: (
+    viewRef: React.ElementRef<ComponentType>,
+    direction: string,
+    preferredX: CodegenTypes.Double
+  ) => void;
   toggleBold: (viewRef: React.ElementRef<ComponentType>) => void;
   toggleItalic: (viewRef: React.ElementRef<ComponentType>) => void;
   toggleUnderline: (viewRef: React.ElementRef<ComponentType>) => void;
@@ -238,6 +272,7 @@ export const Commands: NativeCommands = codegenNativeCommands<NativeCommands>({
     'blur',
     'setValue',
     'setSelection',
+    'setSelectionForVerticalNavigation',
     'toggleBold',
     'toggleItalic',
     'toggleUnderline',
diff --git a/src/EnrichedMarkdownTextNativeComponent.ts b/src/EnrichedMarkdownTextNativeComponent.ts
index e238ecf..0f7b33d 100644
--- a/src/EnrichedMarkdownTextNativeComponent.ts
+++ b/src/EnrichedMarkdownTextNativeComponent.ts
@@ -206,6 +206,12 @@ export interface OnContextMenuItemPressEvent {
   selectionEnd: CodegenTypes.Int32;
 }

+export interface SelectionDragOutsideEvent {
+  direction: string;
+  windowX: CodegenTypes.Float;
+  windowY: CodegenTypes.Float;
+}
+
 /**
  * MD4C parser flags configuration.
  * Controls how the markdown parser interprets certain syntax.
@@ -230,7 +236,9 @@ export interface NativeProps extends ViewProps {
   /**
    * Markdown content to render.
    */
-  markdown: string;
+  markdown?: string;
+  nativeMarkdownBlockId?: string;
+  nativeMarkdownRevision?: CodegenTypes.Float;
   /**
    * Internal style configuration for markdown elements.
    * Always provided with complete defaults via normalizeMarkdownStyle.
@@ -321,6 +329,7 @@ export interface NativeProps extends ViewProps {
    * Receives the item label, the currently selected text, and the selection range.
    */
   onContextMenuItemPress?: CodegenTypes.BubblingEventHandler<OnContextMenuItemPressEvent>;
+  onSelectionDragOutside?: CodegenTypes.BubblingEventHandler<SelectionDragOutsideEvent>;
 }

 export default codegenNativeComponent<NativeProps>('EnrichedMarkdownText', {
diff --git a/src/native/EnrichedMarkdownText.tsx b/src/native/EnrichedMarkdownText.tsx
index 236c2b7..98682ff 100644
--- a/src/native/EnrichedMarkdownText.tsx
+++ b/src/native/EnrichedMarkdownText.tsx
@@ -14,6 +14,7 @@ import type {
   LinkLongPressEvent,
   TaskListItemPressEvent,
   OnContextMenuItemPressEvent,
+  SelectionDragOutsideEvent,
 } from '../types/events';

 export type { MarkdownStyle, Md4cFlags };
@@ -42,6 +43,9 @@ export const EnrichedMarkdownText = ({
   streamingAnimation = false,
   spoilerOverlay = 'particles',
   contextMenuItems,
+  nativeMarkdownBlockId,
+  nativeMarkdownRevision,
+  onSelectionDragOutside,
   ...rest
 }: EnrichedMarkdownTextProps) => {
   const normalizedStyleRef = useRef<MarkdownStyleInternal | null>(null);
@@ -120,8 +124,22 @@ export const EnrichedMarkdownText = ({
     [onTaskListItemPress]
   );

+  const handleSelectionDragOutside = useCallback(
+    (e: NativeSyntheticEvent<SelectionDragOutsideEvent>) => {
+      onSelectionDragOutside?.(e.nativeEvent);
+    },
+    [onSelectionDragOutside]
+  );
+
+  const markdownProps = markdown === undefined ? {} : { markdown };
+  const nativeMarkdownProps =
+    nativeMarkdownBlockId === undefined
+      ? {}
+      : { nativeMarkdownBlockId, nativeMarkdownRevision };
+
   const sharedProps = {
-    markdown,
+    ...markdownProps,
+    ...nativeMarkdownProps,
     markdownStyle: normalizedStyle,
     onLinkPress: handleLinkPress,
     onLinkLongPress: handleLinkLongPress,
@@ -137,6 +155,7 @@ export const EnrichedMarkdownText = ({
     style: containerStyle,
     contextMenuItems: nativeContextMenuItems,
     onContextMenuItemPress: handleContextMenuItemPress,
+    onSelectionDragOutside: handleSelectionDragOutside,
     ...rest,
   };

diff --git a/src/normalizeMarkdownTextInputStyle.ts b/src/normalizeMarkdownTextInputStyle.ts
index 5fd79f8..fdbcbcb 100644
--- a/src/normalizeMarkdownTextInputStyle.ts
+++ b/src/normalizeMarkdownTextInputStyle.ts
@@ -1,8 +1,25 @@
 import { processColor, type ColorValue } from 'react-native';
 import type { MarkdownTextInputStyle } from './EnrichedMarkdownTextInput';
+import { normalizeMarkdownStyle } from './normalizeMarkdownStyle';
 import { normalizeColor } from './styleUtils';

+interface InputBlockStyleInternal {
+  fontSize: number;
+  fontFamily: string;
+  fontWeight: string;
+  color: ColorValue;
+  lineHeight: number;
+}
+
 interface MarkdownTextInputStyleInternal {
+  paragraph: InputBlockStyleInternal;
+  h1: InputBlockStyleInternal;
+  h2: InputBlockStyleInternal;
+  h3: InputBlockStyleInternal;
+  h4: InputBlockStyleInternal;
+  h5: InputBlockStyleInternal;
+  h6: InputBlockStyleInternal;
+  codeBlock: InputBlockStyleInternal;
   strong: {
     color?: ColorValue;
   };
@@ -23,7 +40,31 @@ const DEFAULT_LINK_COLOR = '#2563EB';
 const DEFAULT_SPOILER_COLOR = '#374151';
 const DEFAULT_SPOILER_BG_COLOR = '#E5E7EB';

+const pickInputBlockStyle = (style: {
+  fontSize: number;
+  fontFamily: string;
+  fontWeight: string;
+  color: ColorValue;
+  lineHeight: number;
+}): InputBlockStyleInternal => ({
+  fontSize: style.fontSize,
+  fontFamily: style.fontFamily,
+  fontWeight: style.fontWeight,
+  color: style.color,
+  lineHeight: style.lineHeight,
+});
+
+const defaultMarkdownStyle = normalizeMarkdownStyle({});
+
 const defaultInternal: MarkdownTextInputStyleInternal = Object.freeze({
+  paragraph: pickInputBlockStyle(defaultMarkdownStyle.paragraph),
+  h1: pickInputBlockStyle(defaultMarkdownStyle.h1),
+  h2: pickInputBlockStyle(defaultMarkdownStyle.h2),
+  h3: pickInputBlockStyle(defaultMarkdownStyle.h3),
+  h4: pickInputBlockStyle(defaultMarkdownStyle.h4),
+  h5: pickInputBlockStyle(defaultMarkdownStyle.h5),
+  h6: pickInputBlockStyle(defaultMarkdownStyle.h6),
+  codeBlock: pickInputBlockStyle(defaultMarkdownStyle.codeBlock),
   strong: {
     color: undefined,
   },
@@ -54,7 +95,17 @@ export const normalizeMarkdownTextInputStyle = (
     return cachedResult;
   }

+  const normalizedBlockStyle = normalizeMarkdownStyle(style);
+
   const result: MarkdownTextInputStyleInternal = {
+    paragraph: pickInputBlockStyle(normalizedBlockStyle.paragraph),
+    h1: pickInputBlockStyle(normalizedBlockStyle.h1),
+    h2: pickInputBlockStyle(normalizedBlockStyle.h2),
+    h3: pickInputBlockStyle(normalizedBlockStyle.h3),
+    h4: pickInputBlockStyle(normalizedBlockStyle.h4),
+    h5: pickInputBlockStyle(normalizedBlockStyle.h5),
+    h6: pickInputBlockStyle(normalizedBlockStyle.h6),
+    codeBlock: pickInputBlockStyle(normalizedBlockStyle.codeBlock),
     strong: {
       color: normalizeColor(style.strong?.color),
     },
diff --git a/src/types/MarkdownTextProps.ts b/src/types/MarkdownTextProps.ts
index d450699..986cd4b 100644
--- a/src/types/MarkdownTextProps.ts
+++ b/src/types/MarkdownTextProps.ts
@@ -3,6 +3,7 @@ import type { MarkdownStyle, Md4cFlags } from './MarkdownStyle';
 import type {
   LinkPressEvent,
   LinkLongPressEvent,
+  SelectionDragOutsideEvent,
   TaskListItemPressEvent,
 } from './events';

@@ -25,7 +26,17 @@ export interface EnrichedMarkdownTextProps extends Omit<ViewProps, 'style'> {
    * Markdown content to render.
    * @platform ios, android, web
    */
-  markdown: string;
+  markdown?: string;
+  /**
+   * Native markdown block id to render without passing markdown through JS.
+   * @platform ios
+   */
+  nativeMarkdownBlockId?: string;
+  /**
+   * Native markdown block revision used to invalidate render and measurement.
+   * @platform ios
+   */
+  nativeMarkdownRevision?: number;
   /**
    * Style configuration for markdown elements.
    * @platform ios, android, web
@@ -69,6 +80,7 @@ export interface EnrichedMarkdownTextProps extends Omit<ViewProps, 'style'> {
    * @platform ios, android, web
    */
   onTaskListItemPress?: (event: TaskListItemPressEvent) => void;
+  onSelectionDragOutside?: (event: SelectionDragOutsideEvent) => void;
   /**
    * Controls whether the system link preview is shown on long press (iOS only).
    *
diff --git a/src/types/events.ts b/src/types/events.ts
index 11f876c..1943ff1 100644
--- a/src/types/events.ts
+++ b/src/types/events.ts
@@ -30,3 +30,9 @@ export interface OnContextMenuItemPressEvent {
   selectionStart: number;
   selectionEnd: number;
 }
+
+export interface SelectionDragOutsideEvent {
+  direction: string;
+  windowX: number;
+  windowY: number;
+}
